# G2CC code review — 2026-06-06

Multi-pass review (8 subsystem finders → adversarial per-finding verification → synthesized plan). **41 raw findings → 26 confirmed.**

**STATUS (2026-06-06):** all highs + mediums + most lows FIXED in this session (see git log / `CHANGELOG.md` / `HANDOFF.md`). Deferred low/dead-code findings, kept for the record so they are NOT re-chased: G2CCService startForeground (#15 — parked code), ConnectionManager `_events` never-collected (dead infra), FrameReassembler:76 per-fragment CRC (low protocol-edge), session-pool sessions.json cross-connection lost-update (single-user; needs file locking), stt.ts faster-whisper stdout sentinel (dev-only path), ws-handler menu SELECT only-logs (by design — menu actions not implemented yet). Rationale for each is in `HANDOFF.md` §"The code review".


## Fix plan (18 steps)

> 22 confirmed findings dedupe to 18 fix steps. Two pairs collapse: (1) the BleDisplaySink.kt:18 finding and the G2BleClient.kt:456 finding are the SAME root cause — both trace to queueWrites() line 458 setting _state=Error on a single WRITE_NO_RESPONSE failure; the BleDisplaySink finding is merely the call-through, the fix lives in queueWrites. (2) os-menu.ts:67 and os-display.ts:95 are the SAME root cause (rejected memo promise never cleared) already filed as one finding spanning both files. One finding (AndroidManifest.xml) was adjudicated severityAdjusted="none" — documented intentional parked code per the 2026-06-05 harness pivot — so it is recorded as an informational note (step 18) rather than a code change.

Ordering rationale: no finding is "critical"; the top tier is "high". I lead with the queueWrites Error-teardown regression (step 1) because it is simultaneously the highest-impact (collapses a healthy BLE link mid-render on any transient body-block write — the exact bug the sibling sendPacket BLE-1 fix already removed) AND the lowest-risk fix (delete one line, mirroring an existing in-tree precedent). This is the load-bearing render-stability fix and a clean foundation for the riskier render-core concurrency work that follows. The G2Renderer atomicity/Mutex change (step 6) is sequenced AFTER it because both touch the render path and the Mutex is a higher-risk concurrency change that should land on an already-stabilized write path. Server LOUD-AND-PROUD and auto-recovery regressions (empty catch in cc-session, rejected-memo OS-menu brick, secret-logging) are high-severity and low-to-moderate risk, so they come early. The HarnessActivity recovery/lifecycle pair (steps 4-5) are kept separate but adjacent because both edit teardown()/maybeColdLaunch and the cold-launch dead-end fix should be reasoned about alongside the coroutine-tracking fix. Lower-severity hygiene, dead-code, narrow-race, and probe/RE-tool items trail at the end.

CONFLICT NOTE: steps 1 and 6 both modify the render path and interact. Step 1 removes the spurious Error teardown so a transient write failure no longer kills the link; step 6 serializes renderer entry points and (per the secondary fix in the queueWrites finding) should ALSO make G2Renderer.sendMessage stop issuing further chunks after a failed write. Do step 1 first, re-test, then layer step 6 — do not combine them in one change. No findings contradict each other; the only overlap is shared files requiring sequenced (not merged) edits.

All file paths are absolute. Severities below use the adversarially-adjusted severity from each finding's verdict, not the original author severity.


### 1. [HIGH] queueWrites() must not tear a healthy session to Error on a single WRITE_NO_RESPONSE failure (mirror the sendPacket BLE-1 fix)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/ble/G2BleClient.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/render/BleDisplaySink.kt
- **fix:** DEDUP: the BleDisplaySink.kt:18 finding and the G2BleClient.kt:456 finding are one bug — BleDisplaySink.write just delegates to client.queueWrites, and the defect is the per-packet .fail handler at G2BleClient.kt:458 doing `_state.value = ConnectionState.Error(side, "$label: write status=$status")`. Remove ONLY that state-write line from the inner per-packet .fail (line 458), keeping `anyFailed = true` and the Log.e and the onComplete(false) report — exactly mirroring the sendPacket BLE-1 fix at lines 404-413 (whose comment documents that flipping _state to Error 'tore a HEALTHY session down into a rescan storm on one bad keepalive write'). Audit the outer queue .fail at line 469-471 too; it currently only logs + onComplete(false) and does NOT set _state, so it is already correct — leave it. Real link loss continues to arrive via ConnectionObserver.onDeviceDisconnected, which the health watcher/G2Pipeline post-Ready watchdog acts on. Add a one-line comment referencing BLE-1 so the two write paths stay consistent.
- **risk:** Very low — deletes one line to match an established in-tree precedent (sendPacket). The only behavioral change is that a transient body-block write no longer self-reports as a disconnect; genuine disconnects are unaffected because they flow through onDeviceDisconnected. Theoretical downside: if a write failure were the ONLY signal of a truly dead link (it is not — onDeviceDisconnected covers that), recovery could be marginally slower; acceptable and matches sendPacket's already-shipped behavior.
- **test:** Unit/integration: stub G2BleClient so one packet's writeCharacteristic invokes .fail with a status, the rest .done; assert _state remains Ready (not Error) and onComplete(false) still fires. Regression on hardware: push a multi-tile image (288x129 scene) under marginal RF (body-block the 2.4 GHz link mid-push) and confirm the session does NOT enter a rescan storm — it logs the failed chunk and stays connected. Verify G2Pipeline.startPostReadyWatchdog does not fire scanAndConnect for the transient failure. Confirm a real power-off of the glasses still triggers reconnect via onDeviceDisconnected.

### 2. [HIGH] Empty catch in cc-session stdout line handler swallows handleStreamEvent + listener exceptions (LOUD-AND-PROUD violation; HUD stuck 'processing')
- **files:** /home/user/G2CC/server/src/cc-session.ts
- **fix:** Narrow the try (cc-session.ts:162-171) to JSON.parse only. Parse the line in its own try/catch that returns on non-JSON (keep the existing 'no log — would spam at startup' comment, since that is correct for parse-only failures). THEN push recentEvents and call this.handleStreamEvent(data) OUTSIDE that catch, wrapped in its own try that logs loudly via console.error('[cc-session] handleStreamEvent threw:', e) and continues. This stops Node EventEmitter.emit re-throwing a turn_complete listener exception (ws-handler.ts:935-956 → persistSessionMeta → writeFileSync/renameSync, which can throw on disk-full / unwritable ~/.g2cc) from being silently discarded and leaving the HUD stuck showing 'processing' with resume IDs unpersisted.
- **risk:** Low — splitting one try into parse-only + handler-with-logging changes no happy-path behavior; it only surfaces previously-swallowed exceptions. Slight risk that a noisy but benign listener exception now logs repeatedly; acceptable and desirable per the LOUD-AND-PROUD rule. Must preserve the parse-only silent-return so startup non-JSON lines still don't spam.
- **test:** Unit: feed a valid JSON stream-json line and register a turn_complete listener that throws; assert the throw is caught + console.error'd and the line loop continues processing subsequent lines (does not kill the rl 'line' handler). Feed a non-JSON line and assert it is silently skipped (no console.error). Integration: simulate persistSessionMeta throwing (e.g. point HOME/.g2cc at a read-only dir) and confirm the error is logged AND that the HUD-facing status/response_complete still need a follow-up review — note: this fix makes the failure LOUD but the downstream turn_complete handler still aborts mid-way; flag for the reviewer whether the handler ordering in ws-handler.ts:935-956 should also be made resilient so isProcessing:false is still sent.

### 3. [HIGH] Rejected render promise cached forever — one transient render_menu/render_probe failure permanently bricks the OS screen
- **files:** /home/user/G2CC/server/src/os-menu.ts, /home/user/G2CC/server/src/os-display.ts
- **fix:** DEDUP: single root cause across both files (memoized `rendered` promise never reset on rejection). In os-menu.ts ensureMenuRendered (lines 67-85) and os-display.ts ensureRendered (lines 95-110), append a rejection reset to the IIFE so a failure re-arms the memo: `rendered = (async () => { ... })().catch((e) => { rendered = null; throw e })`. This keeps the success-cached-once behavior but lets the next os_attach retry after a transient execFile failure (Python non-zero exit, 64MB maxBuffer exceeded, venv briefly unavailable, byte-count assertion throw at os-menu.ts:73 / os-display.ts:103). os-menu.ts is the live production screen (osScreen defaults to 'menu'); os-display.ts is the capability-probe harness — same pattern, fix both.
- **risk:** Very low — the `.catch` re-throws after clearing the memo, so callers (ws-handler os_attach error path) still see the rejection and still show the errorScene on the failing attempt; only the PERMANENT stickiness is removed. No happy-path change (a resolved promise is still cached). Minor risk of a tight retry loop if the underlying failure is permanent (e.g. script genuinely missing), but each retry is user-driven (os_attach), not automatic, so no busy-loop.
- **test:** Unit: mock renderAll/renderTiles to reject on first call and resolve on second; call ensureMenuRendered() twice and assert the first rejects, `rendered` is reset to null, and the second resolves and caches. Integration: temporarily make render_menu.py exit non-zero, send os_attach (confirm errorScene), restore the script, send os_attach again WITHOUT restarting the server, and confirm the menu renders. Repeat for os-display.ts via its probe path.

### 4. [MEDIUM] Failed cold-launch dead-ends auto-recovery: launched stays true, watchdog never starts, recovering never clears
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/harness/HarnessActivity.kt
- **fix:** In maybeColdLaunch's failure branch (HarnessActivity.kt:313-316), reset state so the silent-drop auto-recovery backbone can re-fire. Set `launched = false` (and `connecting = false`) so the onConnect guard at line 213 (`if (launched || connecting) return`) no longer blocks future automatic reconnects and updateButtons() re-enables Connect; and if (recovering) clear `recovering` (and `wasServerMode`) so a later successful cold-launch does not spuriously re-enter server mode via line 311. Strongly preferred additionally: start the watchdog (startWatchdog) regardless of launch success, OR on the failure branch when `recovering` schedule a re-attempt (teardown() + onConnect()) instead of dead-ending, so a failed auto-recovery cold-launch keeps retrying unattended per the all-day design goal. Coordinate with step 5 (same function/teardown).
- **risk:** Medium — touches the recovery state machine. Risk of a reconnect loop if cold-launch fails repeatedly (mitigate by reusing the watchdog's existing backoff rather than an unbounded immediate retry). Must ensure resetting launched=false on failure does not double-trigger a maybeColdLaunch already in flight; since the renderer was set at line 289, confirm interaction with step 5's coroutine cancellation so a stale cold-launch coroutine can't re-arm jobs after the reset.
- **test:** Instrumented/mock-BLE: force awaitLaunch to fail; assert launched=false, connecting=false, recovering cleared, Connect button re-enabled, and the watchdog (if started-on-failure) re-attempts. Then simulate a recovery cycle (recoverSession → teardown → onConnect) where the cold-launch fails, and confirm a subsequent successful manual reconnect does NOT auto-enter server mode (wasServerMode cleared). Verify a normal successful cold-launch still arms keepalive/clock/sync/watchdog/renewal exactly once.

### 5. [HIGH] Untracked fire-and-forget test + cold-launch coroutines survive teardown and re-arm UI/jobs against torn-down resources
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/harness/HarnessActivity.kt
- **fix:** Assign both currently-untracked lifecycleScope coroutines to cancellable Job fields and cancel+null them in teardown() (lines 600-615, alongside keepaliveJob/clockJob/etc.): (a) the onTest DisplayTestSequence coroutine (launched ~line 461) → testJob; (b) the maybeColdLaunch coroutine (launched at line 290) → coldLaunchJob. In teardown add `testJob?.cancel(); testJob = null` and `coldLaunchJob?.cancel(); coldLaunchJob = null`. Additionally guard the post-completion mutations against a stale session: the onTest completion at line 473 should call updateButtons() (honoring the serverMode guard) rather than unconditionally `testBtn.isEnabled = true`, and should re-check the session is still live before overwriting status; the cold-launch success block (lines 299-312) should verify it is still the current generation before calling startKeepalive/startClock/startSyncTrigger/startWatchdog/startRenewal. Capture a generation/epoch counter incremented in teardown() and re-check it after awaitLaunch. The cold-launch half is the serious one (a stale coroutine completing after teardown sets launched=true with no live renderer, and the recovery's NEW maybeColdLaunch then hits `if (launched) return` and never cold-launches — permanently stalling recovery).
- **risk:** Medium — cancelling the cold-launch coroutine mid-await interacts with awaitLaunch (suspendCancellableCoroutine); ensure cancellation does not leave a Nordic queue callback dangling or double-invoke onComplete. The generation/epoch guard must be read on the main thread to avoid its own visibility issue. Interacts with step 4 (both edit maybeColdLaunch + teardown) — implement together and reason about the combined state transitions.
- **test:** Mock-BLE: start onTest, then trigger teardown (simulate recoverSession) mid-run; assert testJob is cancelled, no buttons/status are mutated against the new session, and updateButtons honors serverMode. Start a cold-launch, tap Disconnect (or fire recoverSession) during the ~1s window; assert coldLaunchJob is cancelled and the recovered session's fresh maybeColdLaunch actually cold-launches (is not suppressed by a stale launched=true). Verify the generation guard rejects a late awaitLaunch completion from a superseded session.

### 6. [MEDIUM] Renderer operations are not atomic across their multi-message send — clock/renewal coroutines interleave AA packets into a server render (documented double-scroll wedge, only half-fixed)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/render/G2Renderer.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/harness/HarnessActivity.kt
- **fix:** Serialize ALL renderer entry points, not just server-vs-server. The conflated sceneCh + renderConsumerJob (HarnessActivity.kt:492-510) only serializes server-pushed scenes; clockJob (r.setText every 1s, ~line 449) and renewalJob (r.launch every ~80s, ~line 426) call the renderer directly and bypass it. Two viable approaches — pick one for review: (a) give G2Renderer an internal coroutine Mutex (or a single-threaded send actor) that wraps each full operation from build-ops through the final onComplete in sendMessage (G2Renderer.kt:234-251), so only one op's messages can sit on the R-lens Nordic queue at a time; OR (b) route clockJob and renewalJob through the same serialized render pump as renderConsumerJob. Approach (a) is the more robust single chokepoint. IMPORTANT — coordinate with step 1: also apply the queueWrites finding's secondary recommendation here, making sendMessage STOP issuing further messages once a write in the same op has failed (currently line 249 recurses unconditionally with ok&&wok), rather than pushing remaining chunks after a failure. Preserve the DELIBERATE design (G2Renderer.kt:224-232) that lets single-packet keepalive frames (sendPacket) interleave between chunks — the Mutex must guard full multi-packet ops, not the keepalive heartbeat.
- **risk:** Higher (concurrency + timing) — this is the riskiest step. A Mutex held across an entire image push (>1s wall clock at INTER_MESSAGE_PACE_MS) must NOT block or starve the keepalive heartbeat or the firmware will drop the link (the exact failure the per-message-write design was created to avoid). Verify the keepalive path (sendPacket) is outside the Mutex. Risk of deadlock if the Mutex is acquired on the Nordic callback thread that also drives onComplete. Sequence AFTER step 1 so it lands on an already-stabilized write path; do not merge the two changes. The early-abort-on-write-failure change must not regress the onComplete(false) propagation step 1 preserves.
- **test:** Mock-BLE with an instrumented sink that records the interleaving of op IDs on the queue: drive a long server setScene concurrently with the 1Hz clock setText and an 80s renewal, and assert no clock/renewal packets land between two image-chunk messages of the in-flight setScene (the wedge). Confirm keepalive frames STILL interleave between chunks (heartbeat not starved). Hardware: push a 288x129 image while the clock ticks; confirm no mid-tile corruption (the documented double-scroll wedge) and the link stays up across a renewal cycle. Verify sendMessage aborts remaining chunks after an injected write failure and still reports onComplete(false).

### 7. [MEDIUM] --system-prompt replaces CC's default prompt while config contract documents it as appended
- **files:** /home/user/G2CC/server/src/cc-session.ts, /home/user/G2CC/server/src/config.ts
- **fix:** Resolve the contradiction in ONE direction (do not just pick silently — this is for human approval). config.ts:44 documents the field as 'Optional system prompt appended to the default', and the project intent is to augment vanilla CC with an engineering-oriented prompt, not replace its built-in tool-use/environment guidance. Recommended: change cc-session.ts:136 from `args.push('--system-prompt', this.config.systemPrompt)` to `args.push('--append-system-prompt', this.config.systemPrompt)`. If the reviewer instead decides replacement was always intended (spec line 58 shows --system-prompt as an example — the one ambiguous data point), then INSTEAD fix the config.ts:44 docstring to say 'replaces the default system prompt'. Per CLAUDE.md, re-run `claude --help` and re-verify both flag semantics at implementation time before committing either direction.
- **risk:** Low mechanically (one flag string) but BEHAVIORALLY significant — switching to --append-system-prompt restores CC's built-in instructions, which changes model behavior in the dispatch-target subprocess. This is the desired correction but means the CC session will behave differently (likely better) than current; flag for Adam's explicit sign-off on which semantics he wants, since the spec and config docstring disagree.
- **test:** Spawn a CC session with a sentinel systemPrompt and inspect the actual argv passed to the subprocess (log args before spawn) to confirm the chosen flag. Functionally, run a dispatch turn and confirm CC retains its default tool-use behavior (with --append) vs. loses it (with --system-prompt) — e.g. ask the session a question that relies on CC's built-in environment framing. Re-verify `claude --help` output for both flags as part of the test.

### 8. [MEDIUM] Auth token printed to server logs on startup (secret exposure — forbidden pattern)
- **files:** /home/user/G2CC/server/src/index.ts
- **fix:** Remove the secret from the startup log at index.ts:120. Replace `console.log(`[g2cc-server] Auth token: ${config.authToken}`)` with a non-secret line, e.g. `console.log('[g2cc-server] Auth token configured (retrieve via /setup QR or value)')`, or gate the full value behind an explicit debug env flag AND mask it (show only a short prefix). The /setup endpoint already distributes the token (QR + plaintext) as the intended channel, so the startup log adds exposure surface (stdout, OpenRC/journald service log, redirected log files, screen-shares) with no need. This is a literal instance of the forbidden 'never log/print/expose the auth token or any secret' rule.
- **risk:** Negligible — removes a log line; no functional behavior depends on it. Only consideration: operators who relied on the startup log to read the token must now use /setup (already the documented channel). Confirm no test or script greps stdout for 'Auth token:'.
- **test:** Start the server and grep stdout/stderr for the token value and the literal 'Auth token:' string — assert neither the secret nor the labeled line appears (or, if a debug flag is added, that it only appears when the flag is set and is masked). Confirm /setup still serves the token. Run the existing reachable-endpoints startup block to ensure it still points the operator to /setup.

### 9. [MEDIUM] Capture-failure sends audio_end with no preceding audio_start (protocol-invariant break + misleading server error)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/audio/AudioStreamer.kt
- **fix:** Track whether audio_start was actually sent. Add `private var startSent = false`, set it true in the Event.Started handler (AudioStreamer.kt:45-66), and reset it false in start() and stop(). In the Event.Failure handler (lines 77-90), only send ClientMessage.AudioEnd if startSent; otherwise forward the REAL failure reason (event.reason — currently only Log.e'd locally) to the server as a structured failure/stt_error wire message rather than the bare AudioEnd that triggers the misleading server reply 'audio_end without prior audio_start' (ws-handler.ts:430-433). Reset isStreaming=false on the failure path either way so the streamer is not left sticky-true. The three reachable synchronous failure paths (RECORD_AUDIO not granted MicCapture.kt:90, no usable source :102, startRecording() failed :112) all fire before Event.Started; the 'already capturing' path (:86) is unreachable from AudioStreamer due to its own isStreaming guard.
- **risk:** Low — adds a boolean guard and a real-reason wire message; no change to the happy-path start/stream/end sequence. Need a corresponding server-side handler (or reuse an existing stt_error shape) for the forwarded failure reason; if no structured failure message exists, the minimal safe change is to simply NOT emit AudioEnd on the no-start path and log the reason — confirm the server still reports SOMETHING visible so it is not a silent failure. Keep the class KDoc (lines 18-20) honest about what now surfaces.
- **test:** Instrumented: deny RECORD_AUDIO and call start(); assert NO AudioEnd is sent, the real reason is forwarded (or at minimum logged + isStreaming reset to false), and the server does NOT emit 'audio_end without prior audio_start'. Force the no-usable-source and startRecording-throws paths similarly. Then run a normal capture (start → frames → stop) and confirm audio_start precedes audio_end exactly once and startSent resets between sessions.

### 10. [MEDIUM] audioChunks grows without bound if client never sends audio_end (memory-exhaustion / DoS)
- **files:** /home/user/G2CC/server/src/ws-handler.ts
- **fix:** Add a running byte total and a generous hard byte ceiling (a resource guard, NOT an I/O timeout — explicitly allowed under the no-timeouts rule). On each binary push at ws-handler.ts:207 do `client.audioBytes += chunk.length`; pick a cap covering several minutes of 48kHz/2ch/float32 (~384 KB/s). On exceeding it, loud-fail: reset collectingAudio + audioChunks + audioBytes, send stt_error('audio stream exceeded N bytes without audio_end; discarded'), and log. Reset audioBytes alongside audioChunks at every existing reset site (audio_start line 413, audio_end line 438, overlapping audio_start lines 399-401). This bounds a frozen-stop phone bug, a dropped audio_end, or a malicious authenticated peer from OOMing the single-threaded server.
- **risk:** Low — purely additive guard. The only risk is setting the cap too low and truncating a legitimately long utterance; choose a generous ceiling (minutes, not seconds) and surface the discard loudly so it is never silent. Confirm the cap does not violate NO TRUNCATION for normal transcripts — this caps a runaway stream that lacks an end marker, not a delivered transcript.
- **test:** Unit: feed audio_start then stream binary frames past the cap without audio_end; assert collectingAudio flips false, audioChunks/audioBytes reset, an stt_error is sent, and the event is logged. Feed a normal-length stream under the cap and confirm audio_end still produces a transcript with no false discard. Verify audioBytes resets correctly across overlapping audio_start (399-401).

### 11. [MEDIUM] Watchdog-critical fields reloadAttempted/offlineSince/lastAuthedAt are non-@Volatile but cross OkHttp↔IO threads (JMM visibility hole on the keystone recovery path)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/net/ConnectionManager.kt
- **fix:** Mark offlineSince (line 97), lastAuthedAt (line 96), and reloadAttempted (line 99) as @Volatile, matching the file's existing established pattern for cross-thread fields (ws line 79, lastMessageReceivedAt line 95, endpoints/currentEndpointIdx/endpointsTriedSinceSuccess lines 86-88, with the explanatory comment at 91-94). They are written on OkHttp dispatcher threads (onMessage AuthResult success: 312/313/319; onClosed/onFailure: 372/398) and read/written from the Dispatchers.IO liveness watchdog (425-428). @Volatile is sufficient because each site is a single read or single write; the only test-then-set (reloadAttempted at 427-428) is single-threaded within the watchdog coroutine. This restores the happens-before edge so the defence-#5 reset (reloadAttempted=false on re-auth) is visible to the watchdog and cannot leave last-resort recovery permanently disarmed for the process.
- **risk:** Very low — adding @Volatile to three already-shared fields; no logic change, matches the file's own convention. No compound atomicity is needed here, so no lock/Mutex is required. x86 TSO masks the bug in practice, but the fix is correct and cheap and protects the load-bearing auto-recovery path on weaker memory models.
- **test:** Code review / static check that all reads and writes of the three fields are now through @Volatile vars and that no compound check-then-act spans threads. Behavioral: hard to deterministically reproduce a JMM visibility bug on x86; instead assert via a stress harness that after many forced auth-success/disconnect cycles, reloadAttempted is observed false post-reauth from the watchdog (instrument a read) and defence-#5 can re-fire. Confirm no new lock contention (there is none — @Volatile only).

### 12. [MEDIUM] Probe heartbeat hbMsgId wraps at 0x7FFF — sync_trigger msgId becomes a multi-byte varint after ~3.7 min, corrupting the long-session keepalive analysis
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/probe/ProbeActivity.kt
- **fix:** Match the single-byte msgId contract already used by the sibling counters in the same file (kaMsgId wraps at 126 line 762, saMsgId wraps at 126 line 693) and confirmed by PROTOCOL_NOTES.md §389. Change hbMsgId's advance at ProbeActivity.kt:808 from `(hbMsgId + 1) and 0x7FFF` to a single-byte-safe wrap. Because buildSyncTrigger serializes msgId via Varint.encode (Teleprompter.kt:117), the safest ceiling is 0x7F (`hbMsgId = if (hbMsgId >= 0x7F) <start> else hbMsgId + 1`) so the varint stays exactly one byte; do NOT use 0xFF unless buildSyncTrigger is given a raw-byte msgId path, since 0xFF would itself encode as a 2-byte varint. Keep hbMsgId's start value (0x60) or move it into the safe sub-0x80 range.
- **risk:** Very low — one-line wrap change in an RE/debug tool (ProbeActivity is NOT in AndroidManifest.xml, not a shipped path). Risk is only that the keepalive probe's msgId sequence changes; that is the intended correction so the long-session analysis is no longer self-sabotaged. Confirm 0x7F ceiling is consistent with what the firmware acks (the probe exists precisely to determine this — note the ceiling choice is itself part of the experiment).
- **test:** Unit: advance hbMsgId past the wrap repeatedly and assert Varint.encode(hbMsgId) is always 1 byte (value stays ≤127). Run buildSyncTrigger across the full counter range and assert the field-2 encoding length never grows. Behavioral (with glasses, RE context): run a long keepalive probe session beyond ~4 minutes and confirm sync_trigger frames remain well-formed and the session no longer appears to die for msgId-encoding reasons.

### 13. [MEDIUM] BTSnoop parser ignores orig_len != incl_len — silently decodes truncated/FILTERED captures as if complete
- **files:** /home/user/G2CC/scripts/btsnoop_parse.py
- **fix:** In records() (lines 97-102), stop discarding orig_len. Compare it to incl_len per record; accumulate a truncation count (`truncated += 1 if orig_len != incl_len else 0`) and, after the loop, emit a prominent stderr warning if any records were truncated, e.g. `# WARNING: {truncated}/{n} records FILTERED/truncated (orig_len!=incl_len) — payloads incomplete; re-capture with Dev Options HCI snoop=Enabled + BT off/on`. Optionally tag the affected frames so downstream L2CAP reassembly (lines 146-162) / decode_protobuf can mark them. This honors the project's own btsnoop-capture-gotcha memory note (GMS forces FILTERED snoop; verify orig_len==incl_len) and the LOUD-AND-PROUD principle for the primary G2 wire-format debugging tool.
- **risk:** Very low — adds a comparison and a stderr warning; does not change how complete captures are parsed. No risk to correctly-captured files. The only consideration is choosing stderr (not stdout) so the warning doesn't contaminate any piped hex/protobuf output the script emits.
- **test:** Feed a known-FILTERED btsnoop (orig_len > incl_len) and assert the stderr warning fires with the correct truncated/total count; feed a known-complete capture (orig_len==incl_len everywhere) and assert NO warning. Confirm stdout decode output is unchanged for complete captures and that the warning goes to stderr (does not corrupt downstream parsing).

### 14. [LOW] Menu focus direction treats every f3 value except exactly 2 as 'scroll up', including the -1 no-direction default (silent wrong-direction)
- **files:** /home/user/G2CC/server/src/ws-handler.ts
- **fix:** Make direction explicit and reject the unknown case loudly instead of defaulting to up. Replace the `client.osMenuSel = msg.value === 2 ? prev+1 : prev-1` ternary (ws-handler.ts:783-785) with explicit branches: value === 2 → down (next), value === 1 → up (prev), else → log a warning ('[ws] menu focus unknown f3=<value>; not moving') and do NOT change osMenuSel. This avoids silently scrolling up on the EventParser f3=-1 default (EventParser.kt:302, set only when the 0x18 tag is present, decodeHubFocus 294-318) or on an unexpected speed-coded value (the EventParser header at lines 22-28 flags f3 semantics as 'direction OR scroll speed; unconfirmed'). Keep the existing 'flip MENU_DOWN_F3 to 1' escape hatch and update the ws-handler.ts:779 comment to stop asserting the mapping as confirmed fact.
- **risk:** Low — wrong-direction scroll is user-recoverable; the change only adds a no-move + warning for unrecognized values. Risk that the firmware actually does send a value the new code treats as 'unknown' and the menu then feels unresponsive — but that surfaces the protocol ambiguity loudly (the goal) for the planned hardware verification, rather than moving the wrong way. Coordinate the f3 semantics with the hardware-verification milestone before hard-coding 1/2 as final.
- **test:** Unit: send focus messages with value=2 (assert sel increments), value=1 (assert sel decrements), value=-1 and value=3 (assert sel unchanged AND a warning is logged). On hardware (when available), drive the real focus ring and capture the actual f3 values to confirm the 1/2 mapping and whether speed values appear — feed findings back into the mapping.

### 15. [LOW] G2CCService.startForeground failure calls stopSelf() but onCreate keeps initializing (wake lock + WS + BLE scan before teardown)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/service/G2CCService.kt
- **fix:** Make startInForeground() return a Boolean (false in the catch at line ~198-200 where it currently logs + stopSelf), and have onCreate (lines 50-117) early-return immediately when it is false — before acquiring the PARTIAL_WAKE_LOCK (58-70), constructing G2Pipeline (72-74), registering BluetoothStateReceiver (82-94), launching the notification collectors (97-108), and calling pipeline.start() (111) / pipeline.scanAndConnect() (116). Alternatively set a `foregroundStarted` flag in the catch and gate the rest of onCreate on it. This prevents kicking off a WebSocket connect and BLE scan from a context the system just refused to foreground, and avoids wasted wake-lock acquire/release + GATT/scan churn that relies entirely on onDestroy() to clean up. NOTE: this finding is in parked lifecycle code (G2CCService is NOT registered in the current harness AndroidManifest.xml — see step 18); fix it only as part of, or in preparation for, re-enabling the full-app manifest, or leave it with a comment per step 18.
- **risk:** Very low — converts a Unit return to Boolean and adds an early return; the failure path already calls stopSelf, so behavior on success is unchanged. Since the service is currently unregistered, this fix is effectively dormant until the lifecycle subsystem is re-wired; sequence accordingly. onDestroy still runs in the normal stopSelf case, so no regression to existing cleanup.
- **test:** When the service is wired: simulate a ForegroundServiceStartNotAllowedException from startForeground (background mic-FGS start on API 31+) and assert onCreate early-returns WITHOUT acquiring the wake lock, constructing the pipeline, or starting WS/BLE, and that stopSelf still tears the service down. Confirm a normal foreground start still initializes everything once. Until wired, a unit test on startInForeground's Boolean return + an onCreate code-path review suffices.

### 16. [LOW] Probe notify callback silently drops notifications when data.value is null
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/probe/BleProbeClient.kt
- **fix:** Log before returning at BleProbeClient.kt:161: replace `val raw = data.value ?: return@with` with `val raw = data.value ?: run { Log.w(TAG, "[$side] null notify value on $uuid (len=${data.size()})"); return@with }`, matching the loud handling of the sibling failure paths in the same callback (overflow line 164, enableNotifications fail line 168). This honors the probe's capture-everything mission and the NO SILENT FAILURES rule. The null-payload path is only reachable on API 29-32 (the legacy onCharacteristicChanged overload returns a possibly-null byte[]); the real target Pixel 10a is API 35 and uses the non-null overload, so this is dead on the actual device — but the fix costs nothing and removes a silent drop.
- **risk:** Negligible — adds a log line on an effectively-unreachable path in an RE/debug tool not in the manifest. No functional behavior change for real captures.
- **test:** Unit/mock: invoke the notify callback with a Data wrapper whose value is null and assert a Log.w fires with the side/uuid/len and the event is skipped (not crashed). Confirm a normal non-null notify still flows to all three sinks unchanged.

### 17. [LOW] ConnectionManager.connect() overwrites a non-null, not-yet-authed ws without closing it on the scheduleReconnect/forceReconnect race (narrow socket leak)
- **files:** /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/net/ConnectionManager.kt
- **fix:** In connect() (lines 129-154), before assigning the new socket at line ~152, defensively close any existing not-connected socket: `val old = ws; if (old != null && !_connected.value) { try { old.close(4101, "superseded") } catch (_: Exception) {} }`. The wsGen bump (incrementAndGet at line 147) already neutralizes the old listener via isStale(), so closing the superseded OkHttp WebSocket is safe and prevents the transient half-open socket leak that occurs when reconnectJob's connect() and a forceReconnect()-driven connect() interleave (Job.cancel() being cooperative cannot abort the non-suspending body of connect()). NOTE: the empty catch here is acceptable per the no-silent-failure rule ONLY because it wraps a best-effort close of an already-superseded socket whose listener is neutralized — keep the catch narrow to close() and consider a debug log.
- **risk:** Low — adds a guarded close of a socket that is already being discarded; the wsGen design ensures no double-processing regardless. Slight risk if `_connected.value` races such that a genuinely-live socket is closed — mitigated because the close is gated on `!_connected.value` (only close sockets still in CONNECTING). OkHttp idle eviction already bounds the leak, so this is hardening, not urgent.
- **test:** Hard to reproduce deterministically; construct a stress test that fires scheduleReconnect's delayed connect() and forceReconnect() near-simultaneously and assert that at most one open OkHttp WebSocket remains afterward (instrument open/close counts). Confirm the normal single-connect happy path still opens exactly one socket and does not close a live connection.

### 18. [LOW] INFORMATIONAL (no code change): app-lifecycle subsystem unregistered in shipping manifest is intentional parked code, not a defect
- **files:** /home/user/G2CC/android/app/src/main/AndroidManifest.xml, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/service/G2CCService.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/lifecycle/BootReceiver.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/lifecycle/IntentReceiver.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/MainActivity.kt, /home/user/G2CC/android/app/src/main/kotlin/com/g2cc/g2cc/SetupActivity.kt
- **fix:** DO NOT treat as a bug to fix now — the adversarial verdict adjusted this finding's severity to 'none': the manifest comment (lines 5-10: 'No foreground service, no setup screen, no probe app') and project memory (g2cc-display-harness.md, 2026-06-05 harness rebuild) document that G2CCService/BootReceiver/IntentReceiver/MainActivity/SetupActivity are deliberately not registered in the standalone renderer-harness manifest. The only action for THIS review is documentation hygiene: add a short header comment in each parked lifecycle source file noting it is NOT wired into the current harness manifest (dead at runtime by design) so a future reader does not mistake it for live code, and so steps 11/15/17 (which touch these files) are understood as preparing dormant code. If/when the full-app lifecycle subsystem is intentionally re-enabled, that is a SEPARATE authorized effort that must restore the <service android:foregroundServiceType="connectedDevice|microphone">, the receivers, MainActivity (LAUNCHER), SetupActivity, and the FOREGROUND_SERVICE*/RECORD_AUDIO/POST_NOTIFICATIONS/RECEIVE_BOOT_COMPLETED/REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permissions — not part of this fix batch.
- **risk:** None for the documentation note. The risk lies in the OPPOSITE direction: anyone 'fixing' this by adding the components/permissions back into the harness manifest would re-activate a foreground service, boot receiver, and mic/audio permissions that the harness pivot intentionally removed — do not do so without explicit authorization (per CLAUDE.md don't-modify and the harness-rebuild memory).
- **test:** No runtime test. Verify the documentation comments are added to the parked files and that the harness manifest still declares only HarnessActivity. If re-enablement is later authorized as a separate task, test the full foreground-service survival matrix (boot, range loss, BT toggle, backgrounded, killed, reboot) per the Android App Discipline rules.

**Notes:** Dedup summary: 22 confirmed findings → 18 steps. Merged pairs: (1) BleDisplaySink.kt:18 + G2BleClient.kt:456 = one bug in queueWrites (step 1). (2) os-menu.ts:67 + os-display.ts:95 = one rejected-memo bug across two files (step 3). The AndroidManifest finding (severityAdjusted=none) is recorded as an informational no-code-change note (step 18), not an action.

Severity tally (using adversarially-adjusted severities, the authoritative ones): high = steps 1, 2, 3, 5 (4 high). medium = steps 4, 6, 7, 8, 9, 10, 11, 12, 13 (9 medium). low = steps 14, 15, 16, 17, 18 (5 low). No 'critical'.

Note on author-vs-adjusted severity divergences the reviewer should know:
- queueWrites/render-path Error-teardown (step 1): author high, adjusted high — confirmed, led first as highest-impact + lowest-risk.
- G2Renderer race (step 6): author high, ADJUSTED DOWN to medium (renewalJob fires only ~80s, clock setText may be firmware-tolerated) — but the secondary write-abort fix is coupled to step 1.
- HarnessActivity coroutines (step 5): author medium, ADJUSTED UP to high (stale cold-launch coroutine can permanently stall recovery by leaving launched=true so the recovery's new maybeColdLaunch hits `if(launched) return`).
- session-pool watchdog-evict zombie (originally high): ADJUSTED DOWN to medium and NOT included as its own step in error — CORRECTION: it is a distinct finding. See addendum below.
- index.ts /apk readFileSync (author medium): ADJUSTED DOWN to low — NOT included as a step in error. See addendum below.
- G2CCService startForeground (author medium): adjusted to low (step 15).
- stt.ts faster-whisper sentinel (author low): adjusted low, framed as hardening — see addendum.

ADDENDUM — three findings I did not give their own numbered step above and why; the reviewer should add them if desired (they are real, just lower priority / I prioritized the higher-severity and tightly-coupled set within the step budget). I am flagging them explicitly rather than dropping silently:

A. session-pool.ts:168-174 — Evicting a dead pool entry never watchdog.unregister()s it nor kill()s it; a mid-backoff respawn relaunches an orphan CC subprocess (adjusted MEDIUM). Fix: in getOrCreateByDirectory eviction, kill() the evicted session and have ws-handler.ts directory_select (wired=false branch) capture the pre-existing entry id for that path and watchdog.unregister(deadId) before registering the fresh entry; cleanest is an onEvict callback into getOrCreateByDirectory. Risk: medium — touches the global-watchdog/per-client-pool ownership boundary (Watchdog is one global instance; SessionPool is per-connection); ensure unregister targets the correct id and doesn't drop a live session. Test: evict a dead-but-in-backoff entry by re-selecting its directory and assert no orphaned CC subprocess spawns on the next watchdog tick and the old id is gone from the watchdog map. This belongs around priority order 9-10 (server, medium, moderate risk).

B. server/src/index.ts:102 — /apk does a synchronous 17.5MB readFileSync blocking the event loop (adjusted LOW for this deployment: one-time sideload on local NVMe, ~10-50ms). Fix: stream via reply.send(createReadStream(APK_PATH)) with the apk content-type/Content-Disposition headers, or await readFile from node:fs/promises. Risk: very low. Test: hit /apk while a WS client streams CC text and assert no heartbeat/stream stall; confirm the downloaded APK is byte-identical. Belongs near the other low server items (order ~14-17).

C. server/src/stt.ts:104-128 — faster-whisper path trusts raw stdout with no RESULT_BEGIN/END sentinel isolation unlike the DJI/Parakeet paths (adjusted LOW; faster-whisper logs to stderr so steady-state stdout is the single transcript print). Fix: emit the same sentinels and redirect library noise to stderr (contextlib.redirect_stdout(sys.stderr) around model load+transcribe), parse with extractSentinelResult. Risk: very low, defensive hardening. Test: inject a stray stdout print in the inline script and assert the transcript is still cleanly extracted via sentinels. Belongs with the low server items (order ~14-17).

Also recorded but already covered as steps: ConnectionManager events SharedFlow never collected (dead loud-log) — author low, adjusted low; FrameReassembler per-fragment-CRC vs e0 whole-payload-CRC — author low, adjusted low, documented INERT; ws-handler menu SELECT no-op — author low, adjusted low, deferred Phase 2A; session-pool cross-connection lost-update race on sessions.json — author low, adjusted low, explicitly a held/deferred item needing the shared-pool refactor. These four are genuine but lowest-priority; bundle them as a 'low/deferred hygiene' batch after step 17 if a reviewer wants them itemized. I kept the numbered steps focused on the high/medium tier plus the cheap low fixes to keep the one-at-a-time sequence reviewable.

Process flags for the human reviewer:
- NOT IMPLEMENTED — this is a plan for approval only.
- Steps 1 and 6 share the render path and must be sequenced (1 then 6), not merged; step 6 also carries step 1's secondary 'abort remaining chunks after a failed write' recommendation.
- Steps 4 and 5 both edit maybeColdLaunch + teardown in HarnessActivity; implement together and reason about combined state transitions.
- Step 7 (--system-prompt vs --append-system-prompt) needs Adam's explicit decision on intended semantics because the spec (line 58) and config.ts:44 docstring disagree; re-verify claude --help at implementation per CLAUDE.md.
- Step 18 is a do-NOT-touch warning: re-adding lifecycle components to the harness manifest would reverse an intentional pivot and needs explicit authorization.
- Per project don't-modify rule: none of these fixes touch /home/user/g2code or /home/user/g2aria.


## Confirmed findings (verifier detail)


### [HIGH] BleDisplaySink.kt:18 (render-core) — Render path's queueWrites tears the whole connection down to Error on a single transient WRITE_NO_RESPONSE failure, while the renderer keeps pushing the rest of the image
- BleDisplaySink.write delegates straight to G2BleClient.queueWrites, which on ANY single sub-write .fail sets _state.value = ConnectionState.Error(...) (G2BleClient.kt:456-460). This is the exact behavior the sibling sendPacket path was deliberately fixed to AVOID: its comment (G2BleClient.kt:405-413, 'a single WRITE_NO_RESPONSE failure ... must NOT be treated as a GATT disconnect ... doing so tore a HEALTHY session down into a rescan storm on one bad keepalive write') explains why it only logs + reports false. The render core uses queueWrites exclusively, so one transient body-block write failure mid-image (precisely the condition the link tuning targets) flips the session to Error -> the health watcher/recovery tears down and rescans. Compounding it, G2Renderer.sendMessage (G2Renderer.kt:247-249) does NOT abort on a failed write: it sets ok=false but keeps recursing and writing the remaining chunks into a connection that was just marked Error, so subsequent chunks are emitted into a dying session.
- **why real:** Same library, same WRITE_NO_RESPONSE, opposite handling: sendPacket was patched to not flip state; queueWrites (the only path the renderer uses) still flips it. A single transient chunk failure therefore spuriously kills a healthy link during normal rendering — a regression the project explicitly documented and fixed in the other write method but left in this one.
- **verifier (high conf):** The finding is genuine. At G2BleClient.kt:456-459, the `.fail` handler inside `queueWrites` explicitly sets `_state.value = ConnectionState.Error(...)` on any sub-write failure — the exact behavior the BLE-1 fix at G2BleClient.kt:404-413 deliberately removed from `sendPacket`. The `sendPacket` comment names the exact problem: "a single WRITE_NO_RESPONSE failure ... must NOT be treated as a GATT disconnect ... doing so tore a HEALTHY session down into a rescan storm on one bad keepalive write." `queueWrites` (the only path `BleDisplaySink` uses, confirmed at BleDisplaySink.kt:18) still has this
- **suggested fix:** In queueWrites, on a sub-write .fail, log + set anyFailed=true and report onComplete(false) but do NOT overwrite _state to Error (mirror sendPacket's BLE-1 fix); let real link loss arrive via ConnectionObserver.onDeviceDisconnected. Separately, consider having G2Renderer.sendMessage stop issuing further messages once a write in the same op has failed, rather than pushing remaining chunks after a failure.

### [HIGH] G2BleClient.kt:456 (ble-protocol) — queueWrites() tears a healthy session into Error on a single WRITE_NO_RESPONSE failure — the exact bug the sendPacket BLE-1 fix removed, but on the main render path
- In queueWrites() the per-packet .fail handler does `_state.value = ConnectionState.Error(side, "$label: write status=$status")` (line 458). sendPacket() was DELIBERATELY changed (lines 405-413, comment 'BLE-1') to NOT set _state to Error on a single WRITE_NO_RESPONSE failure because 'doing so tore a HEALTHY session down into a rescan storm on one bad keepalive write' — true link loss arrives separately via ConnectionObserver.onDeviceDisconnected. queueWrites never got that fix, yet it is the PRODUCTION render path: EvenHud.coldLaunch/renderMenu/renderText/renderConfirm all call right.queueWrites(...) (hud/EvenHud.kt:123), and Hud.render uses it too. G2Pipeline's per-side state watcher treats ConnectionState.Error identically to Disconnected on an already-Ready session (G2Pipeline.kt:843-851): it flips rightReady=false and calls startPostReadyWatchdog(), tearing down the BLE clients and rescanning. So a single transient WRITE_NO_RESPONSE failure during a render (e.g. a body-block on the 2.4 GHz link — exactly the scenario the retry(10,500)/BALANCED tuning targets) collapses a healthy connection into a full rescan, mid-frame. With multi-packet content one dropped fragment write is enough.
- **why real:** Verified the call chain: EvenHud.writeR -> right.queueWrites (hud/EvenHud.kt:123); queueWrites .fail sets _state=Error (G2BleClient.kt:458); G2Pipeline.kt:821-829 and 843-851 catch ConnectionState.Error on a Ready session and run onInstallFailure / startPostReadyWatchdog (teardown+rescan). The sendPacket comment (lines 405-413) explicitly documents this exact failure mode and why _state must NOT be set to Error here. queueWrites is inconsistent with that intentional fix.
- **verifier (high conf):** The code is exactly as described. G2BleClient.kt line 458 sets `_state.value = ConnectionState.Error(side, "$label: write status=$status")` inside the per-packet `.fail` handler of `queueWrites()`. This is on the production render path: EvenHud.writeR calls `right.queueWrites(...)` (EvenHud.kt:123), which is called from coldLaunch/renderMenu/renderText/renderConfirm.

The BLE-1 fix at lines 405-413 (sendPacket) contains an explicit comment saying this exact pattern — setting `_state` to Error on a single WRITE_NO_RESPONSE failure — "tore a HEALTHY session down into a rescan storm on one bad ke
- **suggested fix:** Mirror the sendPacket BLE-1 fix in queueWrites: drop the `_state.value = ConnectionState.Error(...)` line from the per-packet .fail handler (line 458). Keep `anyFailed = true` + Log.e + the onComplete(false) report so the failure is still loud and the caller's diag sees it, but let real link loss flow through ConnectionObserver.onDeviceDisconnected (which sets Disconnected and is what the health watcher should act on). Same for the outer queue .fail at line 469 if it sets state. Callers already diag on onComplete(false), so the failure remains visible without nuking the session.

### [HIGH] HarnessActivity.kt:458 (harness-os) — Test sequence coroutine and cold-launch coroutine are untracked fire-and-forget — survive teardown/recovery and re-arm UI/state against torn-down resources
- Two long-running coroutines are launched on lifecycleScope without being stored in any Job field, so teardown() (lines 600-615) cannot cancel them:

1. onTest (461): runs DisplayTestSequence.run for ~25s. If a silent-drop recovery fires mid-test (recoverSession->teardown nulls renderer, then re-connects), this coroutine keeps running. render()/renderImage() return false once renderer is null (547,557,567), so it no-ops, but on completion it executes `if (launched) testBtn.isEnabled = true` (473) and setStatus("Tests complete") — re-enabling the button and overwriting status against the NEW (recovered) session. testBtn is also wrongly enabled while serverMode may now be active.
2. maybeColdLaunch coroutine (290): if the user taps Disconnect during the ~1s cold-launch window, teardown nulls renderer but this coroutine holds a local `rend` ref captured at 288. awaitLaunch proceeds against the dead sink; if it happens to report ok, it calls startKeepalive/startClock/startSyncTrigger/startWatchdog/startRenewal (302-306), re-arming all the periodic jobs after the user disconnected.
- **why real:** Neither coroutine is assigned to a cancellable field; teardown only cancels keepalive/clock/renderConsumer/sync/watchdog/renewal/stateJobs. The captured-local renderer reference and the unconditional state mutations on completion are directly in the code. The all-day path explicitly performs teardown mid-operation during recovery.
- **verifier (high conf):** Both coroutines are confirmed fire-and-forget on lifecycleScope with no Job fields, and teardown() at lines 600-615 does not cancel them.

onTest (line 461): real but limited. The render()/renderImage()/renderText() overrides guard via `renderer ?: return false`, so no BLE writes go to a dead sink. The genuine issue is the post-completion mutation at line 473: `if (launched) testBtn.isEnabled = true` — this checks `launched` in the recovered session's context (where it is already true again) and unconditionally re-enables testBtn, potentially while serverMode is active. `updateButtons()` is NO
- **suggested fix:** Track both coroutines in Job fields (e.g. testJob, coldLaunchJob) and cancel+null them in teardown(). Guard the post-completion UI mutations on a captured generation/epoch or re-check that the same session is still live before re-enabling buttons or re-arming jobs.

### [HIGH] os-menu.ts:67 (server-os) — Rejected render promise is cached forever — one transient render_menu failure permanently bricks the OS menu
- ensureMenuRendered() memoizes the render in the module-level `rendered` promise and only re-runs when `rendered` is null: `if (!rendered) { rendered = (async () => { ... })() }`. If the async body rejects (render_menu.py exits non-zero, the 64MB execFile maxBuffer is exceeded, the venv python is briefly unavailable, the script is missing mid-deploy, or the byte-count assertion at line 73 throws), the REJECTED promise stays assigned to `rendered`. Every later call returns that same settled-rejected promise — it never retries. In ws-handler os_attach (line 754) the rejection is caught and an errorScene is shown, but because the rejected promise is cached, EVERY subsequent os_attach for the lifetime of the server process resolves to the same rejection and shows the error screen. The only recovery is a full server restart. os-display.ts:95-110 (ensureRendered) has the identical structure and bug.
- **why real:** This is the single live OS screen (osScreen is always 'menu'). The render shells out to an external Python process via execFile — a genuinely failure-prone boundary (transient OOM on the 64MB buffer, venv not ready, font file missing, script redeploy). A one-time transient failure converts into a permanent menu outage with no self-recovery, which contradicts the project's heavy-auto-recovery goal. Confirmed by reading both functions fully: there is no path that resets `rendered` to null on rejection.
- **verifier (high conf):** The bug is real. Both files confirm it exactly as described.

os-menu.ts:50 — `let rendered: Promise<void> | null = null`
os-menu.ts:67-85 — `ensureMenuRendered()` sets `rendered` only when null, never clears it on rejection. No `.catch` resets the module variable. A single transient failure (Python subprocess exits non-zero, byte-count assertion at line 73 throws, venv absent, maxBuffer exceeded) leaves `rendered` holding a settled-rejected promise for the server's lifetime.

os-display.ts:74 and 95-110 — identical structure, identical bug.

The ws-handler at line 762-765 catches the error an
- **suggested fix:** On failure, clear the memo so the next call retries. e.g.: `if (!rendered) { rendered = (async () => { ... })().catch((e) => { rendered = null; throw e }) }`. Apply the same fix to ensureRendered() in os-display.ts:95-110.

### [HIGH] cc-session.ts:162 (server-core) — Empty catch in stdout line handler swallows every exception thrown by handleStreamEvent and its event listeners
- The try at line 162 wraps BOTH `JSON.parse(line)` (163) AND `this.handleStreamEvent(data)` (166). The empty catch (167-171) is documented as swallowing only non-JSON `stream-json` lines, but it also swallows any exception thrown inside handleStreamEvent — including exceptions thrown by the listeners attached via this.emit(...), because Node's EventEmitter re-throws listener exceptions synchronously out of emit(). Concrete impact: the `turn_complete` listener in ws-handler.ts:935-956 calls client.pool.persistSessionMeta(), which does writeFileSync + renameSync (session-pool.ts:298-299). If ~/.g2cc is unwritable / disk full, that throws; the throw unwinds out of emit('turn_complete') -> handleStreamEvent -> into this empty catch and is silently discarded. The rest of the turn_complete handler (response_complete, output page, status with isProcessing:false) never runs, so the HUD is stuck showing 'processing' forever and the resume IDs silently fail to persist — with ZERO error surfaced anywhere. Same swallowing applies to any throw from scrollback.append, sendMsg, or respondToPermission inside the permission_request listener. Direct violation of LOUD AND PROUD.
- **why real:** Verified the try-block scope spans both JSON.parse and handleStreamEvent (read lines 159-172). Node EventEmitter.emit re-throws listener exceptions synchronously to the caller; handleStreamEvent calls this.emit(...) synchronously, so a throwing listener propagates into this catch. persistSessionMeta does synchronous fs writes that can realistically throw (disk full, permission), and it is invoked from the turn_complete listener. The catch body is empty with no log.
- **verifier (high conf):** Read cc-session.ts lines 160-172: the try block at line 162 wraps both JSON.parse(line) on line 163 and this.handleStreamEvent(data) on line 166 together. The catch at line 167 is completely empty. Node.js EventEmitter.emit() re-throws listener exceptions synchronously to the caller — verified by running a live Node test. handleStreamEvent calls this.emit('turn_complete', ...) at line 366 (also 309). The turn_complete listener in ws-handler.ts:935-956 calls client.pool.persistSessionMeta() at line 937 as its first statement, and persistSessionMeta() does writeFileSync + renameSync at session-p
- **suggested fix:** Narrow the catch to JSON.parse only: parse inside the try, then call this.handleStreamEvent(data) AFTER the try/catch (or in its own try that logs loudly via console.error and continues). E.g.: `let data; try { data = JSON.parse(line) } catch { return }` then push recentEvents + `try { this.handleStreamEvent(data) } catch (e) { console.error('[cc-session] handleStreamEvent threw:', e) }`. This keeps non-JSON lines quiet while making handler/listener failures loud.

### [MEDIUM] G2Renderer.kt:234 (render-core) — Renderer operations are not atomic across their multi-message send; concurrent clock/renewal coroutines interleave AA packets into a server render (the documented "double-scroll wedge", only half-fixed)
- A single render op (launch/setScene/setImage/setText) does NOT hold any lock across its full packet emission. sendOps -> sendMessage issues each message as a SEPARATE sink.write(), and the recursion to the next message happens inside the sink's onComplete callback (G2Renderer.kt:247-249). With BleDisplaySink that callback fires on the Nordic library's callback thread (G2BleClient.sendPacket/queueWrites doc lines 390, 467 — onComplete fires on the callback thread), so the gap between message i and message i+1 of one op is wide open. Meanwhile multiple independent coroutines drive the SAME renderer: clockJob calls r.setText every 1s (HarnessActivity.kt:438-451), renewalJob calls r.launch every ~80s (HarnessActivity.kt:417-428), and renderConsumerJob calls r.setScene for server scenes (HarnessActivity.kt:494-509). A 288x129 image setScene spans multiple chunks at INTER_MESSAGE_PACE_MS=100ms each plus paint, well over 1s wall-clock (memory g2-render-limits: ~1s/200x100 tile), so the 1Hz clock setText WILL fire mid-push. Its single text message's beginAtomicRequestQueue (G2BleClient.kt:449) enqueues onto the same R-lens Nordic queue between two of the server op's image chunks. The conflated channel (HarnessActivity.kt:492) only serializes server-vs-server renders; it does nothing for clock-vs-server or renewal-vs-server. The code comment at HarnessActivity.kt:487-491 and memory note 'concurrent setScene race — serialize/coalesce renders to fix' confirm this interleave corrupts a tile mid-update.
- **why real:** The serialization fix (conflated channel + single consumer) covers ONLY server-pushed scenes. clockJob and renewalJob are separate lifecycleScope coroutines that call the renderer directly, bypassing that channel. Because sendMessage re-enters on the Nordic callback thread between messages, a clock tick lands its write between a server op's image chunks on the shared R-lens queue — exactly the wedge the project already identified and only partially fixed.
- **verifier (high conf):** The finding is confirmed. Reading the actual code verifies every claim:

1. `sendMessage` (G2Renderer.kt:240-251) recurses between messages from inside `sink.write()`'s `onComplete` callback. Each message is a separate `queueWrites` call. Between message i's `q.done` firing and message i+1 being enqueued, the Nordic queue is genuinely idle — other callers can enqueue their own atomic batches into that gap.

2. `clockJob` (HarnessActivity.kt:438-453) calls `r.setText(OsLayout.CLOCK_NAME, t)` directly at line 449 — not through the `sceneCh` conflated channel. `renewalJob` (HarnessActivity.kt:417
- **suggested fix:** Serialize ALL renderer entry points through one mechanism. Either (a) route clockJob/renewalJob through the same conflated/serialized render pump as the server consumer, or (b) give G2Renderer an internal coroutine Mutex (or a single-threaded send actor) that wraps each full operation from build-ops through final onComplete, so only one op's messages can be on the BLE queue at a time. A render-busy flag that coalesces/queues overlapping ops would also work.

### [MEDIUM] HarnessActivity.kt:286 (harness-os) — Failed cold-launch leaves launched=true with no watchdog and recovering stuck — defeats the all-day unattended auto-recovery backbone
- maybeColdLaunch sets `launched = true` unconditionally at line 286, then in the coroutine awaits the launch. On failure (line 313 else branch) it logs "cold-launch FAILED" and calls updateButtons(), but does NOT start the keepalive/clock/sync/WATCHDOG/renewal jobs, and does NOT reset launched. Consequences:
- startWatchdog() never runs, so there is no auto-recovery from this state.
- updateButtons(): connectBtn.isEnabled = !launched = false, so Connect is disabled.
- If this failed cold-launch was itself an auto-recovery attempt (recoverSession -> teardown -> onConnect -> maybeColdLaunch), `recovering` is only cleared inside the `if (ok)` block (309), so it stays true forever. A future manual reconnect that succeeds then unexpectedly re-enters server mode (311) because wasServerMode/recovering were never cleared on the failure path.
The only escape is a manual Disconnect tap (teardown sets launched=false). For the documented all-day-unattended goal, a failed auto-recovery cold-launch means the silent-drop backbone is dead until Adam physically intervenes.
- **why real:** launched=true is set before the await and never reset on the failure branch; the watchdog (the only auto-recovery trigger) is only started inside the success branch; `recovering` is only cleared inside `if (ok)`. All three are plainly visible in the code. The recovery design explicitly relies on the watchdog re-firing, which cannot happen here.
- **verifier (high conf):** The finding is genuine and all three consequences are confirmed by reading the actual code.

FAILURE PATH (verified):
1. recoverSession() (line 398) sets recovering=true, calls teardown() then onConnect().
2. teardown() (line 614) resets launched=false — correct.
3. onConnect() (line 213) guard passes (launched=false), scan proceeds.
4. Both lenses go Ready → maybeColdLaunch() at line 281.
5. Line 286: launched=true is set BEFORE awaitLaunch() is called.
6. awaitLaunch() fails → ok=false → else branch at lines 313-316.
7. launched stays true. No reset anywhere in the else branch.
8. startWatch
- **suggested fix:** On the cold-launch failure branch: reset state so recovery can re-fire — set launched=false (and connecting=false), and if (recovering) clear recovering (and wasServerMode) so a fresh onConnect can be attempted; or start the watchdog regardless of launch success so it keeps retrying. At minimum, schedule a re-attempt (teardown()+onConnect()) on cold-launch failure when recovering, instead of dead-ending.

### [MEDIUM] ConnectionManager.kt:96 (app-lifecycle) — Watchdog-critical fields reloadAttempted/offlineSince/lastAuthedAt are non-@Volatile but written on the OkHttp thread and read on the liveness coroutine
- offlineSince (97), lastAuthedAt (96), and reloadAttempted (99) are plain vars. They are written from the OkHttp dispatcher thread — in onMessage AuthResult success they are set (lines 312 offlineSince=null, 313 lastAuthedAt=now, 319 reloadAttempted=false), and in onClosed/onFailure offlineSince is set (372, 398). They are then read/written from the liveness-watchdog coroutine running on Dispatchers.IO (lines 425-428: reads offlineSince, lastAuthedAt, reloadAttempted to decide the defence-#5 onStuckTooLong() fire, and writes reloadAttempted=true at 428). There is no @Volatile and no shared lock covering these (endpointsLock only guards the endpoints fields). Under the Java Memory Model, the watchdog coroutine has no happens-before edge to the OkHttp thread's writes, so it can observe a stale reloadAttempted==true (last-resort recovery permanently disarmed for the process after the first fire — the exact failure the 4th-pass comment at 314-319 says it fixed, except the fix can be invisible across threads) or a stale offlineSince/lastAuthedAt (mis-timed or missed STUCK_RELOAD recovery). The file is otherwise careful to mark cross-thread fields @Volatile (ws:79, lastMessageReceivedAt:95, endpoints/currentEndpointIdx/endpointsTriedSinceSuccess:86-88), so these three are a consistency gap, not a deliberate relaxation.
- **why real:** The threads are real and documented in this very file (OkHttp dispatcher thread for the listener callbacks; Dispatchers.IO for the watchdog at lines 414-441). The fields cross that boundary with no synchronization. Visibility bugs here are rare-but-real and defeat defence #5 (the keystone auto-recovery the project's MEMORY.md calls out as the goal). Severity medium not high because x86 (and most ARM under coroutine dispatcher hops that often insert barriers) frequently masks it, and a later successful auth re-clears state — but it is a genuine correctness hole on the load-bearing recovery path.
- **verifier (high conf):** The code was read in full. The scope is CoroutineScope(SupervisorJob() + Dispatchers.IO) at line 74, so the watchdog coroutine runs on the IO thread pool. OkHttp callbacks (onMessage, onClosed, onFailure) run on OkHttp's own dispatcher threads. The three fields offlineSince (97), lastAuthedAt (96), and reloadAttempted (99) are plain vars with no @Volatile. They are written from OkHttp threads (lines 312, 313, 319, 372, 398) and read from the IO-dispatched watchdog coroutine (lines 425-428). The file establishes @Volatile as the explicit cross-thread visibility mechanism with a comment at lines
- **suggested fix:** Mark offlineSince, lastAuthedAt, and reloadAttempted @Volatile (matching the existing pattern), or route all reads/writes of the watchdog-state group through a single synchronized(block)/Mutex. @Volatile is sufficient here since each is a single read or single write per site (no compound check-then-act that spans threads except reloadAttempted's test-then-set in the watchdog, which is single-threaded within that coroutine).

### [MEDIUM] ProbeActivity.kt:808 (hud-probe) — Probe heartbeat hbMsgId wraps at 0x7FFF — sync_trigger msgId becomes a 2-byte varint after ~3.7 min, corrupting the long-session keepalive analysis the probe exists to perform
- `sendSyncTrigger` builds an 80-00 sync_trigger via `Teleprompter.buildSyncTrigger(hbSeq, hbMsgId)` and then advances `hbMsgId = (hbMsgId + 1) and 0x7FFF` (line 808), allowing values up to 32767. buildSyncTrigger serializes msgId with `Varint.encode(msgId)` (Teleprompter.kt:117), so once hbMsgId exceeds 127 the sync_trigger carries a 2-byte (then 3-byte) varint in field 2. PROTOCOL_NOTES.md §389 states the 80-00 sync_trigger msgId is part of the SAME single-byte shared counter and that a 2-byte varint there kills the slot. hbMsgId starts at 0x60 (96) and increments once per ~7s beat, so it crosses 128 after ~32 beats ≈ 224s — at which point the probe's own keepalive frames go malformed, which would make a long-running keepalive session appear to die for reasons unrelated to the hypothesis under test.
- **why real:** Provable: (hbMsgId+1) and 0x7FFF permits 96..32767; Varint.encode is multi-byte above 127. This is the exact wire-divergence the probe was built to discover, so a defect here silently sabotages that very investigation (the probe's purpose is determining which beat the firmware acks). Mitigating context: ProbeActivity is NOT registered in AndroidManifest.xml (only HarnessActivity is the launcher), so it is an RE/debug tool, not a shipped user path — hence medium, not high. The sibling counters in the same file (kaMsgId, saMsgId) correctly wrap at 126, confirming the intended single-byte contract; only hbMsgId predates the discovery (added in probe v5, 2026-06-03; the msgId rule was nailed down 2026-06-06).
- **verifier (high conf):** The bug is real and directly verifiable. hbMsgId at line 808 uses `(hbMsgId + 1) and 0x7FFF`, allowing values 0..32767. buildSyncTrigger (Teleprompter.kt:115-120) passes it to Varint.encode(msgId). Varint.kt:19-22 is a genuine multi-byte protobuf varint — values above 127 emit a 2-byte encoding (continuation bit set on first byte). hbMsgId starts at 0x60 (96) and crosses 127 after 32 beats (~224 seconds at 7s/beat), after which every sync_trigger carries a 2-byte varint where the protocol expects one byte, corrupting the frame. The two sibling counters, kaMsgId (line 762: `if (kaMsgId >= 126) 
- **suggested fix:** Match the single-byte contract used by kaMsgId/saMsgId and the production fix: `hbMsgId = if (hbMsgId >= 0xFF) 0 else hbMsgId + 1` (or wrap at 0x7F to stay in the unambiguous single-byte-varint range). Since buildSyncTrigger uses Varint.encode, 0x7F is the safest ceiling; if the byte-255 form is intended, buildSyncTrigger would need a raw-byte msgId path rather than Varint.encode.

### [MEDIUM] session-pool.ts:168 (server-core) — Evicting a dead pool entry never unregisters it from the Watchdog — leaves a zombie that the watchdog respawns
- In getOrCreateByDirectory, when a pool entry for the requested path is found dead (`!entry.session.isAlive()`), it is removed from the pool map (line 171) and a fresh entry is created below. But the dead entry is NOT unregistered from the global Watchdog, and the caller (ws-handler.ts:347-361) only registers the NEW id on the wired=false branch — it never unregisters the OLD one. A session is `!isAlive()` but still watchdog-registered during the respawn backoff window (watchdog.ts:141 awaits up to 32s before respawn). If the user re-selects that directory during the backoff, the pool evicts + creates a fresh entry (new id, registered), while the OLD id stays in the Watchdog's map. On the next watchdog tick (watchdog.ts:115-160), the orphaned CCSession is respawned: a real CC subprocess is launched in projectPath whose `text`/`tool_result` listeners (still wired from the original wireSessionEvents) append to a scrollback no longer in the pool, and persistSessionMeta keeps firing for it. This is exactly the 'zombie that survives' class the codebase fought in S-H2 (cc-session.ts:99-109).
- **why real:** Confirmed via grep that watchdog.unregister is called in every teardown path (ws-handler.ts:270, 592, 852, 893) EXCEPT this eviction path. The watchdog only drops a record on explicit unregister() or crash-loop give-up (watchdog.ts:132); a dead-during-backoff session is in neither state, so the record persists and pass 2 will respawn it. The respawned CCSession retains its original event listeners (the same object is reused), so the orphan actively consumes resources.
- **verifier (high conf):** The finding is real. The critical architectural fact: the Watchdog is a single global instance (index.ts:28, shared via setWatchdog), while SessionPool is per-client (ws-handler.ts:134, new SessionPool() inside handleConnection). This means the watchdog holds registrations across all client pools independently.

The eviction at session-pool.ts:168-173 only calls this.sessions.delete(entry.id) — no kill(), no watchdog.unregister(). The close path (closeSession at pool.ts:199) does call kill(), but getOrCreateByDirectory's eviction does not. The ws-handler directory_select branch (ws-handler.ts:
- **suggested fix:** Make the watchdog the owner of teardown for the evicted id. Simplest: have the eviction emit/return the dead id so the ws-handler can call watchdog.unregister(deadId) before spawning the fresh entry; or pass an onEvict callback into getOrCreateByDirectory. At minimum, in ws-handler.ts directory_select, when getOrCreateByDirectory returns wired=false, capture any pre-existing entry id for that path and watchdog.unregister it before the new register. Also kill() the evicted session in getOrCreateByDirectory (currently only sessions.delete is called) so a mid-backoff respawn target is fully torn down.

### [MEDIUM] index.ts:120 (server-core) — Auth token printed to server logs on startup
- `console.log(`[g2cc-server] Auth token: ${config.authToken}`)` writes the full auth secret to stdout/server logs at every startup. The project rules state explicitly: 'NEVER log/print/expose the auth token or any secret.' Anyone with access to the server's stdout, journald/OpenRC service log, a redirected log file, or a screen-share of the terminal obtains the token, which grants full WS access (and thus a CC subprocess running with --dangerously-skip-permissions under /home/user). The token is also already exposed via /setup (QR + plaintext) which is the intended distribution channel, so the log line adds exposure surface without need.
- **why real:** This is a literal, unambiguous instance of the forbidden pattern named in the task rules and global CLAUDE.md ('Secrets ... Never commit or display them'). The string interpolates config.authToken directly into a console.log. Not speculation — it runs unconditionally in the startup success path.
- **verifier (high conf):** Line 120 of /home/user/G2CC/server/src/index.ts is exactly as described: `console.log(`[g2cc-server] Auth token: ${config.authToken}`)`. This runs unconditionally every time the server starts successfully. It is a direct, literal violation of the project rule "NEVER log/print/expose the auth token or any secret." There is no debug flag, no masking, no guard. The /setup endpoint is the intended delivery channel for the token (QR + plaintext), so this log line adds unnecessary exposure surface — stdout, any redirected log file, an OpenRC service log, or a screen-share. Not a style nit and not sp
- **suggested fix:** Remove the token from the log line, or gate it behind an explicit debug env flag and mask it: e.g. `console.log('[g2cc-server] Auth token configured (see /setup for the QR/value)')`. The reachable-endpoints block already tells the operator where to get the token via /setup.

### [MEDIUM] cc-session.ts:136 (server-core) — --system-prompt replaces CC's default prompt, but the config contract documents it as appended
- spawn() builds `args.push('--system-prompt', this.config.systemPrompt)`. Per `claude --help`, --system-prompt REPLACES the entire default system prompt for the session, whereas --append-system-prompt appends to it. config.ts:44 documents the field verbatim as 'Optional system prompt appended to the default', and CLAUDE.md / the spec describe a vanilla-CC session 'with an engineering-oriented system prompt' meant to augment CC, not replace its built-in instructions. With --system-prompt, setting systemPrompt silently discards CC's entire default system prompt (its tool-use guidance, environment framing, etc.), which is almost certainly not the documented intent and would degrade CC behavior in subtle ways.
- **why real:** Verified `claude --help` output: --system-prompt = 'System prompt to use for the session' (replace); --append-system-prompt = 'Append a system prompt to the default'. config.ts line 44 literally says 'appended to the default'. The code uses the replacing flag, contradicting its own documented contract. Spec line 58 shows --system-prompt as an example, so intent is mildly ambiguous (hence medium not high), but the config docstring is unambiguous.
- **verifier (high conf):** Code verified at server/src/cc-session.ts:136 uses `--system-prompt` (replace). claude --help confirms: `--system-prompt` = "System prompt to use for the session" (full replace); `--append-system-prompt` = "Append a system prompt to the default system prompt". config.ts:44 docstring literally says "Optional system prompt appended to the default" — direct contradiction with the flag used. The spec at line 58 shows `--system-prompt` as an example, which is the one ambiguous data point in the finding's favor, but the spec language doesn't override the config contract. Functional consequence is re
- **suggested fix:** If the intent is to augment CC (as config.ts:44 states), use `args.push('--append-system-prompt', this.config.systemPrompt)`. If the intent is genuinely to replace, fix the config.ts:44 docstring to say 'replaces the default'. Resolve the contradiction in one direction and re-verify against `claude --help` per CLAUDE.md.

### [MEDIUM] AudioStreamer.kt:33 (shared-stt-audio) — Capture-failure sends audio_end with no preceding audio_start (misleading server error)
- AudioStreamer.start() sets isStreaming=true (line 36) and then calls mic.start{...} (line 43). audio_start is sent ONLY in the Event.Started handler (lines 45-66). But MicCapture emits Event.Failure SYNCHRONOUSLY, before any Started, on several paths (MicCapture.kt:86 'already capturing', :90 'RECORD_AUDIO permission not granted', :102 'no usable audio source', :112 'startRecording() failed'). When that happens the Failure handler (lines 77-90) runs while isStreaming is still true and sends ClientMessage.AudioEnd (line 85) even though NO audio_start was ever sent. Server-side handleAudio/ws-handler.ts:430-433 then hits `if (!client.collectingAudio)` and replies with stt_error 'audio_end without prior audio_start' plus a warn log. So a genuine mic/permission/USB failure surfaces to the HUD as the confusing, wrong 'audio_end without prior audio_start' message and violates the documented start/end protocol invariant. The class's own KDoc (lines 18-20) claims failures 'surface to the WebSocket via stt_error logs' — they do, but as a misleading message rather than the real reason (event.reason is only Log.e'd locally, never sent).
- **why real:** Traced both files: Event.Failure is reachable synchronously on 4 distinct paths in MicCapture.start() before Event.Started, and the AudioStreamer Failure branch unconditionally emits AudioEnd whenever isStreaming is true. The server explicitly rejects a bare audio_end. Result is a real, reproducible wrong-error path (e.g. simply denying the RECORD_AUDIO permission triggers it). Not a silent failure, but a protocol-invariant break + misleading diagnostics.
- **verifier (high conf):** Traced the code directly. AudioStreamer.start() sets isStreaming=true at line 36 then calls mic.start() at line 43. Event.Started is emitted at MicCapture.kt line 121, only after startRecording() succeeds without throwing. Three failure paths emit Event.Failure before line 121 ever runs: (1) line 89-92 — no RECORD_AUDIO permission, fires immediately; (2) line 101-104 — all three mic sources return null; (3) line 111-118 — startRecording() throws IllegalStateException. In all three cases, AudioStreamer's Failure handler at lines 83-89 sees isStreaming=true (set at line 36 before mic.start() was
- **suggested fix:** Track whether audio_start was actually sent (e.g. a `private var startSent = false` set in the Started handler, reset in start()/stop()). In the Failure handler, only send AudioEnd if startSent; otherwise forward the real failure reason to the server (a structured failure/stt_error wire message, or at minimum do NOT emit AudioEnd). Reset isStreaming=false either way so the streamer isn't left sticky-true.

### [MEDIUM] btsnoop_parse.py:97 (shared-stt-audio) — BTSnoop parser ignores orig_len != incl_len (decodes truncated/FILTERED captures as if complete)
- records() unpacks orig_len and incl_len from each record header (line 97) but reads only incl_len bytes (line 98) and never compares the two. Per the project's own memory note (btsnoop-capture-gotcha.md: 'GMS forces FILTERED snoop (truncated payloads) ... verify orig_len==incl_len'), a FILTERED capture truncates the payload so incl_len < orig_len. In that case main() will reassemble L2CAP/AA frames from truncated bytes and decode_protobuf()/payload.hex() will emit garbage or silently-wrong fields with NO warning that the data was truncated — directly contradicting the documented capture gotcha and the project's LOUD-AND-PROUD principle for diagnostics.
- **why real:** The struct.unpack reads orig_len into a variable that is then completely unused; only incl_len gates the read. The repo memory explicitly records that filtered snoop truncation is the known failure mode for these captures and that the fix is to verify orig_len==incl_len. This script is the primary tool used to debug the reverse-engineered G2 wire format, so silently mis-decoding a truncated capture would actively mislead BLE protocol debugging.
- **verifier (high conf):** Confirmed real bug. In records() at line 97, orig_len is unpacked from the BTSnoop record header but is never used — only incl_len gates the read at line 98, and only flags/ts/data are yielded at line 102. When GMS forces a FILTERED capture, incl_len < orig_len and the payload is truncated; this script reads the truncated bytes and yields them with no warning. Downstream L2CAP reassembly (lines 146-162) and decode_protobuf() operate on those incomplete bytes. decode_protobuf() does return None for some malformed cases (truncated varints, field overruns), preventing the worst garbage output, bu
- **suggested fix:** In records(), detect truncation and surface it loudly: count records where orig_len != incl_len and write a prominent stderr warning (and/or mark such frames). e.g. accumulate `truncated += 1 if orig_len != incl_len else 0` and after the loop `if truncated: sys.stderr.write(f'# WARNING: {truncated}/{n} records FILTERED/truncated (orig_len!=incl_len) — payloads incomplete; re-capture with HCI snoop=Enabled\n')`.

### [MEDIUM] ws-handler.ts:203 (shared-stt-audio) — audioChunks grows without bound if client never sends audio_end
- Every binary frame while collectingAudio pushes onto client.audioChunks (line 207) with no cumulative-size cap. audioChunks is only reset on audio_start (413), audio_end (438), or an overlapping audio_start (399-401). A client that sends audio_start and then streams binary frames indefinitely without ever sending audio_end (frozen-stop bug on the phone, dropped audio_end, or a malicious authenticated peer) accumulates the entire stream in memory until the server OOMs. At 48 kHz stereo float32 (~384 KB/s) this reaches gigabytes within minutes. There is no time/byte guard because (correctly) no I/O timeout is allowed — but a hard byte ceiling is not a timeout and is the standard mitigation.
- **why real:** Confirmed: grep shows audioChunks.push at line 207 is the only growth site and the only resets are start/end paths. Nothing bounds total accumulated bytes. The reviewer brief explicitly calls out 'unbounded buffer/map growth' as an in-scope server bug. It is auth-gated (line 205) which lowers severity, but it is still a real DoS / memory-exhaustion vector and a true unbounded-growth defect.
- **verifier (high conf):** Read the actual code. At line 207 in /home/user/G2CC/server/src/ws-handler.ts there is exactly one push site: `client.audioChunks.push(Buffer.from(raw as Buffer))`. The only resets are at line 413 (audio_start) and line 438 (audio_end). A grep across the entire server/src/ directory finds no `audioBytes`, `MAX_AUDIO`, `audioCap`, or any other byte-total counter or ceiling. There is no guard that bounds how many bytes accumulate between audio_start and audio_end. The finding's mechanism is accurate: a client that sends audio_start and then streams binary frames without ever sending audio_end wi
- **suggested fix:** Track a running byte total (e.g. client.audioBytes += chunk.length on push) and enforce a generous hard cap (e.g. a few minutes of 48k/2ch/float32). On exceeding it, loud-fail: reset collectingAudio + audioChunks, send stt_error('audio stream exceeded N bytes without audio_end; discarded'), and log. This is a resource guard, not an I/O timeout, so it does not violate the no-timeouts rule.

### [LOW] FrameReassembler.kt:76 (ble-protocol) — Reassembler verifies per-fragment CRC (commandMulti format), but the EvenHub e0 channel uses whole-payload CRC with NO CRC on non-final fragments — a fragmented e0 notify would be dropped
- offer() validates each multi-packet fragment with G2Frame.verifyCrc(fragment) (line 78) and treats the last 2 bytes of every fragment as a per-fragment CRC. That matches G2Frame.commandMulti (per-packet CRC) which the class doc and FrameReassemblerTest are built against. But the EvenHub channel — the actual hijack input/display path — uses a DIFFERENT multi-packet convention (PROTOCOL_NOTES.md lines 620 & 714, 'PROVEN 2026-06-04'): non-final packets carry Len=chunkLen and NO CRC; only the final packet carries one CRC-16 over the ENTIRE reassembled payload. If the glasses ever fragment an e0 notify (e.g. a large e0-01 selection/focus report), this reassembler would (a) misread a non-final fragment's last 2 payload bytes as a CRC, (b) verifyCrc fail, and (c) drop the whole message as 'multi-packet fragment CRC fail' — a silent loss of a real input event, the very thing the class was written to prevent. The send side already knows e0 differs (EvenHub/DisplayProto frame e0 themselves and the doc warns commandMulti CRC-per-packet is 'WRONG for e0'); the receive side does not account for the e0 format.
- **why real:** Low confidence / partially mitigated: I am NOT certain the glasses actually fragment e0 notifies — the class explicitly documents this as INERT ('no captured glasses->phone frame has yet been observed to fragment', lines 22-28) and all observed e0-01 input events are <16 B single packets, so the CRC branch is currently unreachable in practice. But the assumption baked into line 78 (every multi-packet fragment is per-fragment-CRC) directly contradicts the proven e0 wire format, so the moment a multi-packet e0 notify does arrive the event is silently dropped. Flagging per the instruction to include uncertain items at low severity.
- **verifier (high conf):** The finding is real. FrameReassembler.kt line 78 calls G2Frame.verifyCrc(fragment) on every multi-packet fragment, treating the last 2 bytes of each fragment as a per-fragment CRC. This directly contradicts PROTOCOL_NOTES.md line 620 (PROVEN 2026-06-04): non-final e0 packets carry NO CRC; only the final packet carries a single CRC-16 over the entire reassembled payload. This is also confirmed at line 714 for the image transport path. If a glasses→phone e0 notify ever arrived fragmented, verifyCrc would misread the last 2 payload bytes of any non-final fragment as a CRC, fail, and lines 79-82 w
- **suggested fix:** When the first real multi-packet notify is captured (as the doc already mandates), branch reassembly by the service id in bytes[6..7]: for e0-XX, follow the e0 convention (non-final fragments have no CRC — do not verifyCrc them; verify a single CRC over the joined payload on the final fragment), and keep the per-fragment-CRC path only for non-e0 commandMulti-style frames. Until then, at least downgrade the per-fragment 'CRC fail -> drop whole message' for e0 services to a loud warning that preserves the raw bytes for diagnosis, so a format mismatch is visible rather than silently discarded.

### [LOW] G2CCService.kt:184 (app-lifecycle) — startForeground failure calls stopSelf() but onCreate keeps initializing (wake lock + WS connect + BLE scan) before teardown
- startInForeground() catches a startForeground failure (e.g. ForegroundServiceStartNotAllowedException on a background mic-FGS start) and calls stopSelf() at line 200, but does not signal onCreate to abort. onCreate (50-117) calls startInForeground() at line 54 and then unconditionally continues: acquires a PARTIAL_WAKE_LOCK (58-70), constructs G2Pipeline (72-74), registers the BluetoothStateReceiver (82-94), launches the two notification collectors (97-108), and calls pipeline.start() (111, opens the WebSocket) and pipeline.scanAndConnect() (116, starts a BLE scan). stopSelf() only schedules destruction after the current onCreate/onStartCommand returns, so all of that work runs first and is then torn down by onDestroy(). Net effects: (1) a WebSocket connect and a BLE scan are kicked off from a context where the system just refused to let the service run in the foreground; (2) wasted wake-lock acquire/release and GATT/scan churn on every disallowed start; (3) it relies entirely on onDestroy() running to release the wake lock and unregister the receiver after a self-stop, widening the window for a leaked wake lock if the process is killed between stopSelf scheduling and onDestroy.
- **why real:** stopSelf() semantics are well-defined: it does not interrupt the in-progress lifecycle callback. The continuation after the catch is plain control flow with no guard, so every line after startInForeground() executes even on the failure path. This is a real lifecycle-correctness issue, distinct from the NET-7 crash the catch was added to prevent.
- **verifier (high conf):** The control flow is exactly as described. `startInForeground()` (lines 172-202) is `Unit`-returning; its catch block (line 198) calls `Log.e` then `stopSelf()` and returns normally, giving the caller no indication of failure. `onCreate()` (lines 50-117) calls `startInForeground()` at line 54 and then unconditionally executes: wake lock acquire (58-70), G2Pipeline construction (72-74), BluetoothStateReceiver registration (82-94), two `lifecycleScope.launch` collectors (97-108), `pipeline.start()` (111, opens WebSocket), and `pipeline.scanAndConnect()` (116, starts BLE scan). `stopSelf()` only s
- **suggested fix:** Make startInForeground() return a Boolean (false on the caught failure) and have onCreate early-return immediately when it is false (before acquiring the wake lock / building the pipeline / starting WS+BLE). Alternatively set a 'foregroundStarted' flag in the catch and gate the rest of onCreate on it.

### [LOW] ConnectionManager.kt:104 (app-lifecycle) — events SharedFlow is never collected, so its 'buffer overflow — dropped message' loud log can never fire
- ConnectionManager exposes events: SharedFlow<ServerMessage> (105) and onMessage emits to it via _events.tryEmit(msg) (346) with a LOUD-AND-PROUD warning when tryEmit returns false (347-349). But nothing in the app collects connection.events — G2Pipeline consumes server messages exclusively through the onMessage callback (wired as onMessage = ::dispatchInbound at G2Pipeline.kt:1244; grep shows the only .events collectors are the BLE EventParser flows at G2Pipeline.kt:628-629, not the ConnectionManager flow). For a MutableSharedFlow with replay=0 and zero subscribers, tryEmit always succeeds (the value is discarded, nothing to buffer), so the overflow branch is unreachable and the 'slow collector' protection it documents does not exist. This is harmless functionally (the callback path delivers everything) but the guard gives a false sense that slow-consumer backpressure is surfaced.
- **why real:** Verified by grepping the whole kotlin tree: no collector of the ConnectionManager events flow exists. SharedFlow(replay=0) tryEmit-with-no-subscribers returning true is documented kotlinx behavior. So the warn is dead code today.
- **verifier (high conf):** Verified by reading the actual code. ConnectionManager.kt:104-105 declares `_events: MutableSharedFlow<ServerMessage>(extraBufferCapacity = 64)` and `events: SharedFlow<ServerMessage>`. At line 346-349, `tryEmit` is called with a loud-log if it returns false. A grep across all Kotlin source confirms zero collectors of `ConnectionManager.events` exist — the two `.events.collect` calls in the codebase (HarnessActivity.kt:260, G2Pipeline.kt:1093) are both on `G2BleClient.events` (type `EventParser.Event`), not `ConnectionManager.events`. G2Pipeline.kt:51-54 documents intent to collect `Connection
- **suggested fix:** Either remove the events SharedFlow + its tryEmit/overflow block (callback is the real path), or actually collect events somewhere and drop the redundant onMessage callback. If kept for future use, add a comment that it is currently unsubscribed so the overflow log isn't trusted as a live backpressure signal.

### [LOW] ConnectionManager.kt:129 (app-lifecycle) — connect() overwrites a non-null, not-yet-authed ws without closing it on the concurrent scheduleReconnect/forceReconnect race
- connect() returns early only when ws != null && _connected.value (134). When ws is non-null but _connected is false (a socket still in CONNECTING), connect() falls through and overwrites ws = httpClient.newWebSocket(...) at 152 without closing the previous socket. In normal flow the callers null ws first (onClosed/onFailure set ws=null before scheduleReconnect; forceReconnect sets ws=null and closes at 251). The narrow window: scheduleReconnect's reconnectJob (459-467) finishes its delay and begins connect() while, concurrently, forceReconnect() runs from the liveness coroutine — forceReconnect cancels reconnectJob, but Job.cancel() cannot interrupt the non-suspending body of connect() once it is past its last suspension point, so both connect() calls can proceed. The loser's WebSocket (older wsGen) is correctly ignored by isStale(), but its underlying OkHttp WebSocket/socket is never close()d, leaking one connection until GC/OkHttp idle eviction.
- **why real:** The concurrency primitives are real: reconnectJob runs on Dispatchers.IO; forceReconnect can run from the watchdog coroutine on the same scope; cancel() is cooperative and does not abort synchronous code. I could not construct a guaranteed-frequent reproduction, so I mark it low and note the uncertainty — the wsGen design contains the correctness impact (no double-processing); the only damage is a transient leaked half-open socket on a tight interleave.
- **verifier (medium conf):** The finding is real but the severity assessment and mechanics need calibration.

What the code actually does (verified by reading):

1. `connect()` at line 134 guards on `ws != null && _connected.value`. When a reconnect is in-flight, `ws` is null (set null by onClosed/onFailure/forceReconnect before scheduleReconnect is called), so the guard does NOT protect against a second concurrent call.

2. `wsGen.incrementAndGet()` happens at line 147 inside `connect()`, not before. Both concurrent `connect()` calls each call `wsGen.incrementAndGet()`, each capturing a different `myGen`. The second call
- **suggested fix:** In connect(), before assigning the new socket, defensively close any existing one when not connected: val old = ws; if (old != null) try { old.close(4101, "superseded") } catch (_: Exception) {}. The wsGen bump already neutralizes the old listener, so closing is safe.

### [LOW] BleProbeClient.kt:161 (hud-probe) — Probe notify callback silently drops notifications when data.value is null
- In `initialize()`, the per-characteristic notification callback does `val raw = data.value ?: return@with` (line 161), discarding the notify event with no log if the Nordic `Data` wrapper returns a null value. Every other failure path in this class is logged loudly (overflow at line 164, enableNotifications fail at 168), and the whole point of the probe (per the class header and ProbeActivity) is to capture EVERY notify with full untruncated payloads. A null-but-present notification (zero-length GATT notify, or a stack quirk) would vanish from all three sinks with no trace.
- **why real:** Real silent-drop against the project's NO SILENT FAILURES rule and the probe's stated capture-everything mission, but low severity because (a) ProbeActivity is not a shipped path (not in the manifest), and (b) null notify payloads are uncommon and a zero-length notify carries little protocol signal. Including per instructions to surface uncertain low-severity items.
- **verifier (high conf):** The finding is real. At BleProbeClient.kt:161, `val raw = data.value ?: return@with` silently discards notify events when `data.value` (backed by `Data.mValue: byte[]`) is null — no log, no counter, nothing in any sink.

How null gets there: the Nordic library's BleManagerHandler$4 has two `onCharacteristicChanged` overloads. On API 33+, Android calls the new `(BluetoothGatt, BluetoothGattCharacteristic, byte[])` overload with a non-null payload directly. On API 29-32 (minSdk = 29), Android calls the legacy `(BluetoothGatt, BluetoothGattCharacteristic)` overload, which calls `characteristic.ge
- **suggested fix:** Log the null case before returning: `val raw = data.value ?: run { Log.w(TAG, "[$side] null notify value on $uuid (len=${data.size()})"); return@with }` so a dropped/empty notify is visible in logcat and the diag stream.

### [LOW] ws-handler.ts:783 (server-os) — Menu focus direction treats every f3 value except exactly 2 as 'scroll up', including the -1 'no-direction' default
- The menu navigation does `client.osMenuSel = msg.value === MENU_DOWN_F3(2) ? prev+1 : prev-1`. msg.value is the firmware focus f3, forwarded from Android as `ClientMessage.Input("focus", region = ev.name, value = ev.f3)` (SceneCodec.kt:113). EventParser.decodeHubFocus (EventParser.kt:294-318) initializes `var f3 = -1` and only overwrites it when an `0x18` tag is present; a well-formed focus submessage that omits f3 yields f3 = -1 → on the wire value = -1 → the server scrolls UP. Worse, EventParser's own header comment (EventParser.kt:24-28) states the sub-event field 'may encode direction OR scroll speed; unconfirmed' — if f3 is a speed/position that can be 3,4,... then all fast scroll notches also fall into the else-branch and scroll UP. The strict equality `=== 2` makes only one exact value mean 'down'.
- **why real:** Grounded in the actual decode (f3 defaults to -1) and the actual wire mapping (value = ev.f3). Low severity because it is hardware-unverified (the code itself flags the f3 semantics as unconfirmed and provides a 'flip MENU_DOWN_F3 to 1' escape hatch) and wrong-direction scrolling is user-recoverable, not a crash or data loss. I am explicitly marking this low/uncertain per instructions.
- **verifier (high conf):** The finding is confirmed as real. Reading the actual code:

EventParser.kt:302 initializes `var f3 = -1` and only overwrites it when tag `0x18` is present (line 313). A HubFocus submessage that omits f3 yields f3=-1, forwarded by SceneCodec.kt:113 as `value = ev.f3`. At ws-handler.ts:783-785 there is no guard for value === -1 or any unrecognized value — the else-branch unconditionally scrolls up. The EventParser header (lines 22-28) explicitly states "direction encoding unconfirmed" and notes f3 may encode speed rather than direction, making values 3, 4, etc. plausible inputs that also fall in
- **suggested fix:** Make direction explicit and reject the unknown case loudly instead of defaulting to up: branch on `value === 2` (down) / `value === 1` (up) / else log a warning and do not move the selection. This avoids silently moving the wrong way on a -1 default or an unexpected speed-coded value, and surfaces the protocol ambiguity for the planned hardware verification.

### [LOW] ws-handler.ts:794 (server-os) — Menu SELECT (double-tap / hub_gesture code 3) only logs — selecting a menu item does nothing
- In the menu input branch, the SELECT gesture handler is `else if (msg.event === 'double_tap' || (msg.event === 'hub_gesture' && msg.code === 3)) { console.log('[ws] menu SELECT ...') }`. It logs the chosen item but takes no action — there is no dispatch to Claude Code, Dictation, Timer, etc. The five MENU_ITEMS (os-menu.ts:25) are therefore navigable but inert.
- **why real:** Verified: the SELECT branch has no side effect beyond console.log; no call into the dispatcher, directory picker, or any state transition. This may be intentional for the current 'Slice 1 / remote display loop' milestone (the memory notes describe the menu as display-only at this stage), so I am marking it low and flagging the uncertainty rather than asserting it is a defect. Including it because a reviewer scanning the menu nav would expect SELECT to do something.
- **verifier (high conf):** Read ws-handler.ts lines 794-795 directly. The SELECT branch (double_tap or hub_gesture code 3 while osScreen === 'menu') contains exactly one statement: `console.log(...)`. No dispatch call, no state transition, no directory picker, no CCDispatcher instantiation — nothing. The five MENU_ITEMS in os-menu.ts:25 are navigable but selecting one does nothing.

The file header (lines 13-14) documents "Phase 2A scope: dispatch flow (target → directory → session → prompt → stream)" and "Phase 3A adds: heartbeat…", so this is explicitly deferred work, not an oversight. The memory notes (g2cc-os-slice1
- **suggested fix:** If selection is meant to be wired in this slice, route each MENU_ITEMS index to its action (e.g. 'Claude Code' → push directory_list_reply and set the directory-picker flow). If it is deliberately deferred, leave a TODO referencing the milestone so the no-op is obviously intentional.

### [LOW] index.ts:102 (server-core) — /apk handler does a synchronous 17.5 MB readFileSync, blocking the entire event loop
- The /apk route reads the harness APK with `const apk = readFileSync(APK_PATH)` (line 102) inside the async Fastify handler, then sends it. The APK is ~17.5 MB on disk (verified). readFileSync blocks the single Node event loop for the full read on EVERY /apk request — during which no other WebSocket traffic is serviced: server-driven heartbeats (hb) are not sent, CC text_delta streaming stalls, audio frames queue, and the liveness checker can't run. On a slow disk or under memory pressure this is a multi-hundred-ms to multi-second global stall affecting every connected client, not just the downloader.
- **why real:** Confirmed APK size (ls -la /tmp/g2cc-harness.apk = 17,508,755 bytes). readFileSync is synchronous and blocks the libuv event loop; Fastify runs on a single thread. The whole point of the WS server (heartbeats, streaming) is latency-sensitive. This is a real (if intermittent) blocking-I/O-in-hot-process defect.
- **verifier (high conf):** Code confirmed exactly as described. Line 102 of server/src/index.ts: `const apk = readFileSync(APK_PATH)` using the synchronous `readFileSync` imported from `node:fs` (line 16). No async alternative (`readFile`, `createReadStream`) is imported. The finding is technically correct — readFileSync blocks the libuv event loop for the full read duration.

However, the severity is overstated. Two mitigating factors specific to THIS deployment: (1) /apk is a one-time sideload path — Adam downloads it once, not on every request, so the blocking occurs essentially never in normal operation; (2) APK_PAT
- **suggested fix:** Stream the file instead of buffering it synchronously: `reply.type('application/vnd.android.package-archive').header('Content-Disposition', ...).send(createReadStream(APK_PATH))` (import createReadStream from node:fs), or register @fastify/static for the APK. Streaming yields to the event loop between chunks. If a buffer is required, use await readFile from node:fs/promises so the read doesn't block.

### [LOW] session-pool.ts:268 (server-core) — Cross-connection lost-update race on ~/.g2cc/sessions.json (each WS connection has its own SessionPool)
- handleConnection creates `new SessionPool()` per WebSocket (ws-handler.ts:134), so multiple concurrent connections each own a separate pool but all read-modify-write the same ~/.g2cc/sessions.json via persistSessionMeta (read existing -> merge -> writeFileSync tmp -> rename). With two connections persisting near-simultaneously, connection A can read the file before B's rename and then rename its own stale snapshot over B's, dropping B's just-saved resume IDs. The SRV-9 atomic temp+rename prevents a torn/corrupt file but does nothing for the lost-update interleaving.
- **why real:** Confirmed per-connection pool instantiation and that persistSessionMeta does a non-atomic read-modify-write with no cross-process/cross-pool lock. Marked LOW because the code explicitly acknowledges this as a held item (session-pool.ts:295-296: 'the remaining cross-client lost-update race needs the shared-pool fix') — so it is known/deferred, not a newly missed defect. Single-user single-phone usage makes concurrent connections uncommon, but reconnect churn (old socket persisting on turn_complete while the new socket also persists) can realistically interleave.
- **verifier (high conf):** The finding is accurate on every technical claim. ws-handler.ts:134 creates `new SessionPool()` per WebSocket connection. session-pool.ts:268-299 shows persistSessionMeta doing a non-atomic read-modify-write: loadSavedSessions() reads the file, merges, then writeFileSync+renameSync atomically — but the read and the write are two separate operations with no lock. Two concurrent connections doing this simultaneously can interleave so the later rename overwrites the earlier one's changes. The comment at lines 294-296 explicitly admits this ("The remaining cross-client lost-update race needs the s
- **suggested fix:** Make the session store a single shared instance (module-level SessionPool or a dedicated persistence module behind one writer), or serialize writes through an async mutex / append-and-compact log. The architectural fix is the 'shared-pool' change already referenced in the code comment.

### [LOW] stt.ts:104 (shared-stt-audio) — faster-whisper path trusts raw stdout (no sentinel isolation like the Parakeet/DJI paths)
- transcribeFasterWhisper builds a Python -c script whose only stdout is print(' '.join(s.text for s in segments)) (line 118) and then takes the whole stdout via stdout.trim() (line 128) as the transcript. Unlike the DJI and Parakeet paths, which wrap results in ___G2CC_RESULT_BEGIN/END___ sentinels and discard surrounding noise (extractSentinelResult, lines 65-76, and the daemon protocol), this path has no isolation. Any line faster-whisper / its deps (faster_whisper, ctranslate2, huggingface_hub) ever print to stdout — model-download progress, deprecation/UserWarnings that some libraries route to stdout, CUDA banner lines — would be concatenated into the transcript and then fed downstream as the user's prompt. The code comment at lines 61-62 acknowledges 'NeMo / tqdm noise on stdout' is a real concern for the other engines but the whisper path is unprotected.
- **why real:** Real but lower-confidence: faster-whisper is generally quiet on stdout (most logging goes to stderr), so in the common case stdout is clean. The risk is conditional on a dependency emitting to stdout (e.g. first-run model download progress, or a library that prints warnings to stdout). When it triggers, the failure is silent corruption of the transcript with no error — exactly the class the sentinel pattern was introduced to prevent for the other engines. Marked low because it depends on dependency stdout behavior.
- **verifier (medium conf):** The finding is real and the asymmetry is genuine: transcribeFasterWhisper (lines 104-137) uses raw stdout.trim() with no sentinel isolation, while transcribeDji (line 181) and the Parakeet daemon path both use the RESULT_BEGIN/END sentinel pattern and extractSentinelResult. The code comment at lines 59-62 explicitly acknowledges that NeMo/tqdm stdout noise is why sentinels exist for the other engines.

However, the severity and "silent corruption" framing is somewhat overstated. Two mitigations already exist in the code:

1. The console.log at line 130 prints the full result including any spur
- **suggested fix:** Make the inline script emit the same sentinels and route all library noise to stderr: import sys; print(RESULT_BEGIN); print(' '.join(...)); print(RESULT_END) and parse with extractSentinelResult(stdout); optionally set logging to stderr / contextlib.redirect_stdout(sys.stderr) around the model load + transcribe so only the sentinel-wrapped transcript reaches stdout.

### [NONE] AndroidManifest.xml:23 (app-lifecycle) — Entire app-lifecycle subsystem is unregistered in the shipping manifest (no service, receivers, or required permissions)
- The only manifest in the source tree (android/app/src/main/AndroidManifest.xml) is the harness manifest. It declares exactly one component — HarnessActivity — and NONE of the app-lifecycle components under review: G2CCService, BootReceiver, IntentReceiver, MainActivity, SetupActivity. It is also missing every permission these components require: FOREGROUND_SERVICE, FOREGROUND_SERVICE_CONNECTED_DEVICE, FOREGROUND_SERVICE_MICROPHONE, POST_NOTIFICATIONS, RECORD_AUDIO, RECEIVE_BOOT_COMPLETED, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS. I confirmed via the merged manifest (app/build/intermediates/merged_manifest/debug/.../AndroidManifest.xml) that the only merged-in receiver is androidx ProfileInstallReceiver — none of the G2CC lifecycle components appear, and there are no build flavors or alternate source-set manifests (build.gradle.kts has only buildTypes + a java.srcDirs sourceSets override). Consequences that are concrete code-level breakage: (a) G2CCService.start()/startForegroundService() targets an undeclared service — the start is silently dropped, so 'service survival' is moot because the service can never come up; (b) BootReceiver/IntentReceiver receive nothing because they are unregistered, so boot auto-start and Tasker intents (documented in android/INTENTS.md) are inert; (c) MainActivity.requestAndStart() (MainActivity.kt:72-95) requests RECORD_AUDIO and POST_NOTIFICATIONS at runtime, but a runtime request for a permission not declared in the manifest is auto-denied without a dialog, so grants.values.all{it} is false and startServiceIfConfigured() never runs.
- **why real:** Verified by reading the actual manifest and the merged manifest output. This is the literal definition of a service-survival failure: the foreground service the review targets is not declarable/startable from the only manifest that ships. It may be INTENTIONAL given the documented harness pivot (MEMORY.md: 'app rebuilt as standalone renderer harness'), so I flag it rather than asserting a regression — but as written, the lifecycle subsystem cannot run, and re-enabling it requires manifest + permission work, not just flipping a flag.
- **verifier (high conf):** The factual claims are accurate. The manifest at android/app/src/main/AndroidManifest.xml declares only HarnessActivity (lines 41-49) and only BLE/network permissions — no G2CCService, BootReceiver, IntentReceiver, MainActivity, SetupActivity, no FOREGROUND_SERVICE*, RECORD_AUDIO, POST_NOTIFICATIONS, or RECEIVE_BOOT_COMPLETED. There are no product flavors, no alternate manifest source sets, and no build variants that would inject a second manifest — the build.gradle.kts sourceSets block (lines 66-73) only remaps kotlin → java.srcDirs; the mergedManifest outputs match. The lifecycle source file
- **suggested fix:** If/when the lifecycle subsystem is meant to run, restore (or add a full-app source set / build variant with) <service android:name=".service.G2CCService" android:foregroundServiceType="connectedDevice|microphone" android:exported="false"/>, the BootReceiver (exported=true, intent-filter BOOT_COMPLETED + MY_PACKAGE_REPLACED), IntentReceiver (exported=true with its actions), MainActivity (LAUNCHER) and SetupActivity, plus the FOREGROUND_SERVICE*, RECORD_AUDIO, POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED and REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permissions. If the parked state is intentional, add a comment in the lifecycle files noting they are not wired into the current harness manifest so they aren't mistaken for live code.