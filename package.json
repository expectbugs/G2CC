{
  "name": "g2cc",
  "private": true,
  "version": "0.0.1",
  "description": "Direct-BLE G2 glasses + Claude Code dispatch + DJI single-mic learned-profile NR (+ NLMS fallback) + Parakeet ASR",
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=24"
  }
}
