<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      G2CC — Display Renderer Test Harness.
      Standalone BLE connect + auth + render to the G2 glasses, driven by three buttons
      (Connect / Test Display / Disconnect) + a Diag checkbox. No foreground service, no
      setup screen, no probe app — auth token + Tailscale server are baked in via BuildConfig.
    -->

    <!-- BLE: connect to + scan for the lens pair. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <!-- BLE scanning on API < 31 requires location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Network: stream the diagnostics log to the server. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:name=".G2CCApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.G2CC"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- The harness IS the app — the only launcher activity. -->
        <activity
            android:name=".harness.HarnessActivity"
            android:exported="true"
            android:label="G2CC Harness">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
