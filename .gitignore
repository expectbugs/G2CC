node_modules/
dist/
.env
config.json
*.log

# Harness build secrets (auth token baked into the APK via BuildConfig — never commit)
harness-secrets.properties

# Python
audio/venv/
__pycache__/
*.pyc
.pytest_cache/

# Audio captures (real workplace recordings — keep out of git)
audio/samples/*.wav
audio/samples/*.flac
audio/samples/*.json

# Claude Code session state
.claude/

# Gradle build artifacts
.gradle/
android/build/
android/app/build/
local.properties

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Build artifacts
*.tsbuildinfo
scripts/simtools/*.so

# ff1-play harness runtime output (scenes, logs) — the tool regenerates these
server/tools/ff1-play/data/play.log
server/tools/ff1-play/data/mirror.log
server/tools/ff1-play/data/mirror.stdout
server/tools/ff1-play/data/scenes.jsonl
server/tools/ff1-play/data/scene-latest.json
